# traxgen — Plan

This file holds the project's **operational state**: where things stand, what's in flight, where it's going, and in what order it gets there. The architecture — what a layer is, how the pieces compose — lives in `CLAUDE.md` and the README. The project's purpose and direction live in `project-instructions.md` and `vision.md`. This file is the live state in between.

---

## Living document discipline

Keep this file current at session close, as part of the close routine in `workflow.md` — that routine owns the *how*; this is just what to touch here:

- **Status changed?** Update Current state.
- **Item completed?** Remove it from Sequenced work in flight.
- **New work surfaced?** Add it to Sequenced work, naming its sequencing strategy.
- **Session done?** Append an entry to the Session log.

This file is **operational state** only. Don't duplicate `decisions.md` (locked choices) or `observations.md` (recurring process patterns) — point at them, and let each be the source of truth for its own domain.

## Current state

Phase 2, first item done. `generate --board standard-square` emits a four-plate course with the goal across a plate boundary, the app certified it 2026-09-14 (s36), and the record carries the row -- so the command now reports `claim: CONNECTED` and `--measured-only` emits the same 253 bytes. The emulator renders on the GPU when launched with `-gpu host` and refuses a software renderer at boot (see *What is true now*). Allostatik routines at **v0.3.9** (upgraded s34). Milestone status:

| Milestone | Status |
|---|---|
| M1 Foundation (types, hex, inventory) | done |
| M2 Domain + parser | done |
| M3 Round-trip (byte-compare) | done (bbf7e36) |
| M4 Validator | done — 12/12 v1 rules; 3 Phase-2 rules deferred, 2 dropped. **`START_GOAL_CONNECTED` added 2026-08-10** as the 13th registered check, unblocked by M5.c |
| M5 Generator | M5.b-minimal done (2d89e77); M5.b-fix done (f6e5717) — `generate_minimal()` emits the validated no-rail adjacency shape, app-certified via `FLW4TMLP5V`. **M5.c done 2026-08-10** — `traxgen/graph.py` holds the goal-side rotation rule, the complete six-row live-direction table, and three-valued `ConnectionStatus`. **M5.a partially done** — `PlacedTile` + world-coordinate iteration + pair classification exist; no traversal, no multi-hop path yet. **s22 2026-08-21: `graph.py` is plate-aware** — claims answer from `MEASURED_RUNS` only, the conjunction model lives on its own `predict_*` surface, and new `traxgen/plates.py` owns the footprints. **s35 2026-09-14: multi-plate placement built** -- `generate_multi_plate()` behind `--board standard-square`, 33 predicted placements over 11 distinct starter->goal pairs, emitted UNMEASURED. **s36 2026-09-14: app-certified** -- rendered active and recorded in `MEASURED_RUNS`, so it now reports CONNECTED |
| M6.a Upload | done — `uploader.py`, mock-server tests + live canary |
| M6.b Round-trip verification | **done 2026-08-08** — connection semantics resolved. `g = (d + 1) % 6` is a *goal-side* rule, confirmed on every active cell ever measured with zero exceptions. What varies is the *live direction set*, which moves with the starter's `hex_rotation`: exhaustively mapped at s=0 (E rot 1, NW rot 3) and s=1 (NE rot 2 only). Characterizing s=2..5 is open unknown #15, not a blocker. **2026-08-21: the "moves with the starter's `hex_rotation`" reading is superseded** — the live set is a conjunction of the starter's port parity and which neighbouring cells are on the baseplate; see answered #15 |
| M6.c Automated render verification | done — Android harness + play-button validity oracle, ~25s/code |

First traxgen-generated course certified valid by the app: share code `FLW4TMLP5V` (no-rail STARTER + GOAL_RAIL adjacency shape). The validated geometry: STARTER@(0,0) rot 0 + GOAL_RAIL@(-1,0) rot 3, `rail_count = 0`.

**What is true now.** Three findings out of the retired narrative that the next session still needs:

- **The connection model takes the goal’s plate.** `predicted_live_directions` and `predict_connection` require `goal_plate_offset` with no default, reproduce all eleven recorded rows with no free parameters, and carry no support term — pinned by a test that it stays out. (2026-08-26, s28.)
- **The board is a lattice, and some holes are half.** Plate arrangement has basis `(5,0)` and `(3,-6)`, making `STANDARD_SQUARE` exactly `{0,1}×{0,1}`. A lone plate has **30 addressable cells and only 27 usable**: on alternating rows the outermost hole’s centre lies on the plate boundary and takes no piece until a neighbouring plate completes it. Corpus-checked at `f876ca5`. (2026-08-24, s26.)
- **Frame comparison must use `BOX`.** Pillow’s default `BICUBIC` understates a real change by 22% and `NEAREST` reports 0.000 for a one-pixel moving line — attenuated motion reads as a still screen. `frame_white_fraction` carries the same latent bug, named rather than fixed, because changing its filter would invalidate its 0.942-vs-0.014 calibration. (2026-08-24, s26.)

- **The generator crosses a plate boundary, and 11 distinct shapes are reachable.** On `STANDARD_SQUARE` the model calls 33 cross-plate placements live, collapsing to **11 distinct starter->goal pairs**; goals reach plates 1 and 2 only, since plate 3 sits diagonally from the starter's plate and shares no cell boundary. Committed at `bb1ea4f`. (2026-09-14, s35.)
- **No course can differ from the certified one by a single term.** At rotation 0 the starter's three ports face the even directions, and from the certified cell (0,0) all three land on its own plate -- so crossing a boundary from that cell requires an odd rotation (1, 3 or 5 only). The search therefore holds rotation at 0, the best-covered value in the record, and moves the starter cell instead. Pinned by test. (2026-09-14, s35.)
- **The model's first cross-plate prediction is certified.** STARTER local (-4,0) rot 0 on plate (0,0), GOAL_RAIL local (-6,5) rot 5 on plate (3,-6), direction SW: `predict_connection` called it live, the generator emitted it UNMEASURED, and the render came back active with nothing in the record fitted to it -- D032's standard, met by the product rather than by an experiment. (2026-09-14, s36.)
- **The emulator renders on the GPU when told to, and the boot grade refuses it when it doesn't.** With `-gpu host` the device reports `Android Emulator OpenGL ES Translator (Apple M1 Pro) ... (4.1 Metal - 90.5)`, read from `dumpsys SurfaceFlinger` (D081); cold boots 20.6-23.6s against 26.8-46.7s before. The flag is necessary: s37 booted `auto` and then `host` on the same loaded Mac, 17 minutes apart, and got SwiftShader and then Metal (#19, answered). One cold boot in three still lost a frame during the menu wait in s36, and in s37 three `--fresh` renders on a loaded Mac all failed in the app's first seconds, each a different way -- so the renderer was not the whole story behind s35's freezes (#20). (2026-09-14, s36; 2026-09-21, s37.)

*Retired from plan.md at s34* in `knowledge/docs/plan-parked.md` — the dated narrative for s21 through s33 that used to sit here. The fuller, canonical account of each of those sessions is in `log.md`.

Artifact status: nothing was lost with the deleted `~/Desktop/Hub` checkout — `scripts/trace_both.py` and both oracle fixtures (`4YCV8JHLX7.course`, `X3WEQ6F296.course`) are committed and present in the fresh clone (verified via project tree, 2026-07-22).

## Phase 1 definition of done

`python -m traxgen generate --set vertical-starter` produces a `.course` binary that: (1) is accepted by the share-code upload endpoint, (2) loads in the official app via that code without errors, (3) uses only PRO Vertical Starter-Set (26832) pieces, (4) has a valid ball path from a starter to a goal, (5) fits on the set's 4 baseplates. Phase 1 closure is measured against this.

**Explicit v1 non-goals:** interesting tracks, variety, physics simulation, aesthetics, race mode, perpetual mode. Phase 1 proves the pipeline works end-to-end — nothing more.

**PHASE 1 CLOSED 2026-09-07 (s32).** On the command as written: `python -m traxgen generate --set vertical-starter` -> 177 bytes -> upload endpoint -> `KN6F459ZR3` -> rendered `active` by `render_course.py --fresh` on a cold boot with the main menu recognised before the first tap. Clauses (1)-(5) measured; (3) and (5) are true of `generate_minimal()` by construction. Closed rather than the DoD amended, per D070: moving the finish line to match a queue just shown to be pointed elsewhere would be the drift. **Phase 2's target is the generator** -- see Sequenced work.

## Sequenced work in flight

**Order as of s37: one render first, then 3 -> 8 -> 1 -> 2 -> 7 -> 10 -> 9.** Items 14 and 17 are done. The render: `--fresh` on the control `KN6F459ZR3` right after a Mac restart. Every render since s36's restart has run on a Mac short of memory, and all three in s37 failed in the app's first seconds (#20); a pass is #20's cheap test and 17(d)'s first live proof, and a fail says the phone's own memory is next. Numbers are stable references, so nothing below is renumbered. Items 4, 5, 6, 11, 12, 13, 14, 15 and 17 are done or retired; 16 is where the lifecycle, polling and sidecar work consolidates.

12. ~~**Tile-kind terms in `MeasuredRun.lookup_key`, required with no default**~~ — **DONE 2026-09-08 (s33)** at `b45d57d`; see Current state and D074. Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.
13. ~~**App-version check in `preflight.py`, and the version in every sidecar**~~ — **DONE 2026-09-09 (s33)** at `c124d72`, the check half (D075); the version-in-every-sidecar half moved to item 16, because eight scripts write sidecars and a field in each is the copy that item retires. Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.
14. ~~**The generator, inside the measured envelope**~~ — **DONE 2026-09-14 (s36)**: certified by render and recorded in `MEASURED_RUNS`; `--measured-only` now emits the same 253 bytes. Body under *Retired 2026-09-21 (s36)* in `knowledge/docs/plan-parked.md`; the account is in `log.md` (s35, s36).
15. ~~**The five record defects from the 2026-08-28 assessment**~~ — **DONE 2026-09-07 (s32)** as canonical-file edits: `plan. Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.
16. **Harness extraction: campaigns adopt `emulator.session()` and `wait_for_main_menu`, and their own `SETTLE_SECONDS` sleeps retire** — s32 built the lifecycle and the menu wait and wired them into `render_course.py --fresh` only, deliberately: the three near-duplicate campaign loops (two share 84 of 99 lines, the third 41) are exactly where a fourth copy would come from. Rides with item 3's polling wiring, since both replace fixed sleeps in the same flow. Also: log `MenuArrival` per run (the number `SETTLE_SECONDS` used to guess; since s37 `render_course.py` prints it on arrival through `on_menu`, D085, and the campaigns still do not), `tests/test_graph.py` should stop importing from `scripts/` (assessment item 13), and **every sidecar records `app_version`** (item 13's second half, s33 -- `android.read_app_version` exists; the eight sidecar writers are the copies this item consolidates). **And `render_course` writes no sidecar at all** (s36): a share code exists only in terminal output, which is why s36's `MEASURED_RUNS` row cites the course's bytes and hash instead of its code (#52). *relatedness* — one harness batch.

17. ~~**Get the emulator rendering on hardware, and make the harness report device death honestly**~~ — **DONE 2026-09-21 (s37)**: (a) and (b) in s36 (D081, D082); (c), the watchful sleep, folded into item 3 unbuilt (D086); (d) built -- the main-menu wait skips and counts an unreadable frame and stops only on three frameless screencaps in a row (D084), and a render reports what the wait saw even when a later step fails (D085). **Not yet proven live:** no render passed in s37 -- three `--fresh` runs on a loaded Mac failed upstream of the wait (#20) -- so the opening render of the next session is also (d)'s first live proof.

1. **Design the port-taxonomy instrument** — the attack on open unknown #7, and next session's opening design work (design, not locked design — lock what survives into `decisions.md` then). The 93-member `TileKind` catalog killed pairwise sweeping in one command: 93² pairs × 36 cells ≈ 311k renders. The decoding unit is the **port** — a (side, height, direction-of-travel) opening — and kinds collapse onto a small archetype vocabulary (13 members are baked-in rotation/state variants of three pieces; curves and tunnels almost certainly share an interface; the mirror collapse is *not* assumed — symmetry models have been refuted by render twice). Three instruments, cheapest first: **(a) corpus mining** — the format speaks ports natively for rails: every rail serializes two `RailConstructionExitIdentifier`s carrying `side_hex_rot` and `exit_local_pos_y` (verified in murmelbahn source, 2026-08-10), so parsed shared courses yield (tile, side, height) attachment points off disk — free, positive-evidence-only. **(b) Vision reads** — the app renders all 93 kinds regardless of ownership; contact-sheet courses screenshotted through the harness and read by Claude yield per-piece port hypotheses, committed as auditable artifacts (screenshot beside structured read), allowed to prioritize the render queue, never to write `graph.py`'s table — vision-predicted cells stay `UNMEASURED` until rendered. **(c) Chain probes** — starter→X→goal_rail renders confirm; a dark button blames the chain, not a joint (conjunction), so hold one joint known-good and vary the other. **Pre-registered calibration first:** compose vision reads of STARTER and GOAL_RAIL; test whether they predict the measured six-row table. Suspicion declared in advance: geometry under-predicts parity — either outcome moves us (observations #20, #21). **Gate:** the zoom probe — at the default camera a two-tile course is a sliver of the frame (observations #18); port reading needs pixels; the first experiment is whether scripted zoom or the editor view yields legible close-ups. *learning*. **2026-08-18 (s20): instrument (a) is built, run at full corpus scale, and calibrated** — 43,375 observations / 74 kinds / 652 courses, zero renders; see the session-log row. Its calibration passed (both oracle fixtures parse to the certified geometry; `classify_pair` returns CONNECTED). Port-record storage shape and corpus policy are locked in `decisions.md`. What remains of item 1: the vision-channel gate (zoom probe), chain-probe oracle semantics (well-formed vs completes-the-run), and where the committed record store lives in the repo.
2. **M5.a proper: traversal over the track graph** — `graph.py` currently classifies *pairs*; a real track graph needs edges between arbitrary placed tiles and a walk from starter to goal. Blocked in practice by open unknown #7 (per-tile-type connection rules): the STARTER→GOAL_RAIL mechanism is measured, everything else is not — item 1 is the attack. *dependency* — the next generation step (`generate --set vertical-starter` producing something bigger than two tiles) needs it.
3. **Wire polling into `render_course`** — `wait_until` / `wait_for_node` exist and are tested offline; the four native steps still use fixed sleeps. *blast radius* — smallest change that retires the 2026-08-07 stopgap. **Urgency restored 2026-08-10**: a locked screen throttled the occluded emulator mid-queue and a fixed sleep fired into a loading screen (the frame guard caught it, one auto-resume closed it). Polling would have absorbed the throttle instead of tripping on it, which is exactly the unattended-run failure mode the queue runner otherwise removes. **2026-08-23 (s23) scope correction:** this item does *not* close the splash-screen failure. `wait_for_node` reads `uiautomator`, and the Unity game surface is opaque to it — the same blindness the foreground guard hit, one layer on. `wait_until` takes an arbitrary `dump_fn`, so the primitive fits; the predicate has to be pixel-stability, per the triggered review below. The s23 run hit exactly this: the opening certified control was refused at `white_frac 0.660` (a loading screen), costing a whole campaign and one re-run. **2026-08-24 (s26): the predicate now exists** — `android.wait_for_stable_frame` (`4de50a8`) — so what remains of this item is the wiring: replacing the four fixed sleeps in `render_course` with the appropriate wait, `wait_for_node` for the `uiautomator`-visible steps and `wait_for_stable_frame` for `after_load` / `after_render_load`. Note the predicate is necessary rather than sufficient (a splash screen is still), so the wiring keeps the frame guard and the foreground guard rather than replacing them. **2026-09-07 (s32) status — still not wired:** as of `841ca73` nothing outside the tests calls `wait_for_stable_frame`, `render_course` still sleeps fixed `WAITS`, and `FRAME_STABILITY_TOLERANCE` is still the declared 0.0 because the calibrating live run never passes through it. The s32 frames show the main menu *animates*, so tolerance 0.0 would never pass there — the calibration is not a formality. **2026-09-21 (s37): 17(c) folds in here (D086)** -- once the fixed sleeps are polls, a device death is reported against the step that was polling, which is all the watchful sleep was for. Also: s37 saw truncated frames arrive three in a row while the app starts, and `wait_for_stable_frame` refuses any gap by design; if they arrive during a course load too, this item needs a rule for them (restarting the quiet streak, not skipping, is the candidate).
4. ~~**Record the 2×2 into `MEASURED_RUNS`, and correct the model**~~ — **DONE 2026-08-26 (s28)**: two rows (not five -- arms sharing a lookup key share a row), graded against the run's committed sidecar; and the model corrected on the term the record forced, `goal_plate_offset` rather than the plate set. Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.

   ~~**Run the #17 2×2**~~ Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.
5. ~~**The half-hole render experiment**~~ — **DONE 2026-08-26 (s28)**: `WINDOW_ONLY`. Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.
6. ~~**A base layer that models the baseplates instead of assuming them**~~ — **RETIRED 2026-09-07 (s32)**: its own text says what remained was item 4's question, which is answered on both axes (#17). Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.
7. **The two remaining s24 panel recommendations, each enacted green and each a real change** — (a) `build_variant` delegating to `build_course`: −39/+12 lines, and it makes the sweep's byte-identity guards real (a generator-drift mutation goes from 1 failure to 4; `_assert_control_matches_generator`'s docstring currently admits it cannot catch that drift, and delegation is what makes it false in the good direction). (b) Moving `STANDARD_SQUARE` out of `plates.py`, whose stated concern is which local cells each `LayerKind` occupies, not how several plates sit together — note s25 moved a constant *into* that module for the opposite reason (`BASEPLATE_LAYER_KINDS` is a per-`LayerKind` fact; `STANDARD_SQUARE` is not), so the two moves are one consistent rule rather than two tastes. *relatedness* — one epistemics-and-cohesion batch. The third recommendation, `MeasuredRun` recording the plate set, **landed 2026-08-24 (s25)**.
8. **Teach the library the SkyTrax era** — supersedes the old "accept v7, skip the u32=13" framing (that u32 turned out to be a relocated *field*, not a constant — see answered #13). Per murmelbahn upstream `8d7974a`: `CourseSaveDataVersion` 6–7, `CourseElementGeneration` ids 10–13, `TileKind` ids 94–126, and the skytrax course body (generation after metadata; layer `(id, kind, position, int small-stacker height)`; connector array; sha256 tail) — `types.py` + parser, source commit noted per `.cursorrules`. Rides with it: commit `scripts/mine_ports.py` (+ offline tests) and the harvest/probe scripts, per the locked corpus policy. *relatedness* — one schema-fidelity batch, now real upstream-documented work rather than a byte-skip.
9. **Re-verify `local_hex_position` honoring under the harness** — promote the tentative finding to resolved or falsify it; the manual-loop evidence is tainted by the clipboard failure. *bake-time* — cheap now that sweeps are routine.
10. **Dedicated lint-cleanup session** — the 72 pre-existing ruff findings (re-counted 2026-08-24 by stashing s24's files and re-linting; the older "38" figure was scoped differently), plus whatever the three new scripts added, plus 2026-08-10's four new files. *relatedness* — batch as one session; not mixed into feature work. Note the standing practice that keeps this bounded: each session lints only its own new files (2026-08-10 verified its four were clean and confirmed the rest pre-existed by stashing), so the debt is inherited, never grown.

11. ~~**Cycle the emulator per working session instead of leaving it up**~~ — **DONE 2026-08-28 (s30)**: `scripts/emulator. Body under *Retired from plan.md at s34*, in `log.md` under *Retired 2026-09-11 (s34)*.

## Triggered reviews

- monthly → check the tool's CHANGELOG.md for a newer release

- When a milestone completes → refresh the README's status paragraph (it drifts otherwise). **Fired 2026-09-07 (Phase 1 closed) and missed at that close; discharged 2026-09-09 (s33).**
- When murmelbahn's schema changes upstream → update `types.py` to match and note the source commit in the docstring (per `.cursorrules`). **Fired 2026-08-18** — upstream `8d7974a` (PR #28) adds the SkyTrax era: v7 body, generation ids through 13, TileKind through 126. Scoped into Sequenced work item 8 (SkyTrax; this said "item 5" until s32 -- the number was reused); retire this note when it lands.
- When `ROTATION_OUT_OF_RANGE` appears to false-positive on an app-accepted course → revisit the modulo-normalization hypothesis (open unknown #11).
- ~~When rendering in the emulator gets laggy → upgrade the harness from fixed sleeps to polling-based waits.~~ **Fired 2026-08-07**; stopgap applied and the polling primitives built. **Fired again 2026-08-10** via a different route — GPU throttling of an occluded emulator window, not general lag. Retire this row once Sequenced work item 3 (polling) lands.
- When the two Unity-surface waits (`after_load`, `after_render_load`) start failing → they cannot be polled via `uiautomator` (the game surface is opaque to it); a pixel-stability predicate is the only route. Not yet triggered — the IME step is what failed. **Built ahead of the trigger 2026-08-24 (s26)**: `wait_for_stable_frame` exists and is tested offline, but the two waits still run on fixed sleeps until item 3 wires it in. Retire this row when that lands.
- Monthly → check Allostatik's `CHANGELOG.md` for a release newer than v0.3.4; an upgrade is the `UPGRADING.md` walk under Part 1's *Upgrade contract* (placed 2026-08-27, s29).

## Open questions

Still-open v1-scope unknowns, numbering preserved from the original PLAN.md for traceability (resolved/dissolved items live in the archived PLAN and in `decisions.md` where they hardened):

- **#1 Switch TileKind encoding** — state via TileKind vs separate field; verify by parsing an app-exported single-switch course.
- **#2 Rail `side_hex_rot` semantics** — de-prioritized 2026-06-12 (not the minimal-course blocker); matters again when the generator places explicit rails. **2026-08-10:** corpus mining (Sequenced work item 1) will read `side_hex_rot` at scale for free — distributional evidence arrives as a by-product. **2026-08-18: it did** — 43,375 observations; `side_hex_rot` is a world-frame cell edge, and subtracting the tile's `hex_rotation` recovers piece-intrinsic structure (26 kinds collapse to a single parity class of relative edges). The tile-relative reading is the meaningful one.
- **#3 Baseplate physical shape** — **answered 2026-08-21, from the corpus, zero renders.** A `BASE_LAYER_PIECE` occupies exactly **30 local cells** — `y ∈ [-6,0]`, `x ∈ [0,5]`, rows of 2/4/6/6/6/4/2 — and **no course ever uses a position outside that set**. Derivation and the regenerating script are parked in `knowledge/docs/plan-parked.md`; read them when a footprint constant is in question.
- **#4 Balcony world-coord resolution** — probe-first follow-up; until then balcony-touching rails skip span validation.
- **#5 Retainer ID assignment scheme** — ranges known, scheme unknown.
- **#6 GUID generation** — GUID=0 accepted by upload; app-side validation unverified beyond current evidence.
- **#7 Connection rules per tile type** — feeds the track graph; derive from physical specs + fixtures. **2026-08-10 sharpened:** 93 `TileKind` members make naive pairwise sweeping dead on arrival (~311k renders at 36 cells/pair); the decoding unit is the **port** — a (side, height, direction-of-travel) opening — and the attack is Sequenced work item 1: corpus-mined rail ports, vision reads of app renders, chain-probe confirmation, calibrated first against the measured STARTER→GOAL_RAIL table. **2026-08-18: the corpus instrument ran at scale** — 74 kinds observed; 26 show clean single-parity-class intrinsic port sets (19 even, 7 odd, none mixed); exit heights are perfectly quantized (`y = 0.27 + 0.36k`, 8 levels, zero exceptions in 43,375 records); the spread-class kinds are bimodal, cause open. Catalog note: upstream `TileKind` now runs to id 126 (SkyTrax pieces 94–126), so the 93 figure was already stale — the port reframe gets more necessary, not less.
- **#8 `THREE_ENTRANCE_FUNNEL` mapping** — best-guess; confirm from a fixture using the piece. **2026-08-18: corpus evidence** — 34 observations across real courses put its entrances at relative edges {0,2,4}, balanced. Three entrances at alternating edges; render has never tested it, but the mapping is no longer a guess.
- **#9 Track-graph representation** — design when #7 is answered enough to be worth it.
- **#10 `LayerKind` semantics** — `BASE_LAYER` vs `BASE_LAYER_PIECE` distinction; what `LARGE_GHOST_LAYER` is.
- **#11 Rotation modulo hypothesis** — see triggered review above.
- **#12 Which upload headers are strictly required** — full set works; stripping untested.
- **#13 Schema v7 delta** — **answered 2026-08-18.** v7 is the SkyTrax format; the mystery `u32 = 13` is `CourseElementGeneration::SkyTrax`, the generation field relocated to just after metadata. The decomposition and the murmelbahn pointer (`8d7974a`, `lib/src/app/skytrax.rs`) are parked in `knowledge/docs/plan-parked.md`; read them when Sequenced item 8 starts.
- **#14 Why four of six adjacent directions have no connecting rotation** — **answered 2026-08-08**, and superseded by #15. The starter's `hex_rotation` was pinned at 0 for all 42 cells of the 2026-08-07 sweep; the four dead directions were dead *at that starter rotation*, not in general. Working-out parked in `knowledge/docs/plan-parked.md`.
- **#15 What determines the live direction set, and why does its size change?** — **ANSWERED 2026-08-21.** `live(s, pos) = plate_available(pos) ∩ {(r + s) % 6 for r in STARTER_PORTS}`, with `STARTER_PORTS = {0,2,4}` (corpus, n=380) and `plate_available` read off the 30-cell footprint (corpus, n=28,494). The superseded framing and the refuted symmetric model are parked in `knowledge/docs/plan-parked.md`.
- **#16 Baseplate arrangement is assumed, not modelled** — **arrangement half answered 2026-08-24 (s24), zero renders**: **228 of the 241 four-plate courses use exactly `{(0,0), (3,-6), (5,0), (8,-6)}`**. **Still open:** "plates connect only parallel-side to parallel-side" is Colby's physical observation, not something read off the corpus. The three dated restatements, the lattice basis and the eleven four-plate shapes are parked in `knowledge/docs/plan-parked.md`.

- **#17 Is "off-plate" physical, or outside a canonical coordinate window?** — **ANSWERED 2026-08-25 (s27): the answer is the coordinate window**, with a third state found at s26 — cells **addressable in the format and refused by the app editor** (the half-holes), at **1 tile on an uncompleted half-hole against 2050 on completed ones**. Consequence: **the generator must address every tile on its owning plate**, and **connection composes across plate boundaries**. The four dated design iterations and the residue note are parked in `knowledge/docs/plan-parked.md`.

**Deferred cleanup** (small, non-blocking — from PLAN.md, trimmed to what's still live): the list is parked in `knowledge/docs/plan-parked.md`. It comes back when a cleanup session runs, or when Sequenced item 8 (SkyTrax) picks up the `CourseElementGeneration` ids it names.

- **#18 Does #17's residue note still point at a live item?** — new 2026-09-12 (s34). The parked #17 body ends "Residue, and it is item 5's", but Sequenced item 5 was retired 2026-08-26 (s28). Either the residue moved to another item or it is discharged; reading the parked body against the current Sequenced list settles it in one pass. *Trigger:* whoever next opens #17's parked body.

- **#19 Why does this AVD render in software even with the GPU enabled?** — new 2026-09-14 (s35). `hw.gpu.enabled` was `no` and `hw.gpu.mode=auto`; setting `enabled=yes` left the log still reporting SwiftShader. `spawn_emulator` passes no `-gpu` flag, so an explicit `-gpu host` is untested and is the next probe. Until this is answered every render on this machine is a CPU rasterisation of a Unity game. **Answered in practice 2026-09-14 (s36), not in mechanism:** with `-gpu host` the device reports Metal on the M1 Pro (D081). But that boot also followed a Mac restart, which clears the memory pressure s35 measured, so whether the flag is *necessary* is confounded. **One boot separates them:** `uv run python -m scripts.emulator boot --gpu auto` reproduces s35's launch on a restarted machine -- Metal means the restart did it, SwiftShader means the flag did. `-gpu host` stays either way. **ANSWERED 2026-09-21 (s37): the flag.** The Mac was 6 days past that restart and back at s35's squeeze (61.6 MB free, 6.56 GB held by the memory compressor), so the planned boot could not separate the two on its own: `--gpu auto` came up on SwiftShader (`ANGLE (... SwiftShader Device ...)`), which both causes predict. A `-gpu host` boot 17 minutes later on the same Mac (63.0 MB free, 6.66 GB) got Metal. One variable changed and one result changed: the flag is necessary on a loaded Mac, and enough there. Whether `auto` finds the GPU right after a restart is untested and moot while D081 keeps `-gpu host`.
- **#20 Are the app freezes caused by software rendering alone, or do they need the host's memory pressure too?** — new 2026-09-14 (s35). Three freezes in five runs, on a 16 GB host carrying ~8 GB in the memory compressor with ~64 MB of free pages, running a 2 GB AVD. Software rendering is measured; the memory reading is a suspicion tied to no specific failure. Answering #19 first probably settles this one for free. **Sharpened 2026-09-14 (s36), and not settled for free:** on the GPU, one of three cold boots still lost a frame -- a truncated 541,526-byte screencap at main-menu poll 8, about 16s into the menu wait. So software rendering is not the whole story. The cheap test is item 17(d)'s tolerant poll plus a count of how often it fires across cold boots. **s37, on a loaded Mac (59.6-63.0 MB free, 6.56-6.97 GB compressor, 6 days up), no render passed, and each failure was different:** the `--gpu auto` boot's second adb call failed `error: fork failed: Out of memory` right after `boot_completed` -- bionic's wording (macOS says `Cannot allocate memory`), so the phone's side ran out; the first `host` render lost three frames in a row at main-menu polls 5-7, truncated at 1,115,806, 336,434 and 763,034 bytes (with s36's 541,526 at poll 8, all four losses landed about 8-16s into the menu wait, while the app starts); the second, at a load average of 53.5, found the launcher in front and could not say when the app left (the repair is D085); the third, next morning at 5.2, died on `error: device still authorizing` from a menu-wait screencap -- the adb connection reset after the device checks had passed. Candidates, none tested: the phone running out of memory while the app starts, which fits all four; the Mac's squeeze, since s36's freshly restarted Mac lost one frame across several renders; and 17(d)'s own polling, which now keeps taking screenshots through the window where the old wait died. **The cheap test is the same render right after a restart** (the order line above); if it still fails, the AVD's `hw.ramSize`, never read into the record, is the next thing to look at.

## Session log

Session log → allostatik/log.md (moved s31, v0.3.7 upgrade).

## Cross-references

- `project-instructions.md` — identity, purpose, mode, and domain context (Layer 2).
- `workflow.md` — the session routines (open, drift-check, close, capture, handoff).
- `decisions.md` — locked choices, with the reasoning behind them.
- `observations.md` — recurring process patterns.
- `vision.md` — the project's longer-arc direction.
- `knowledge/` — project-scope reference material (Layer 3).
- `docs/refs/` (repo) — the project's committed reference corpus, indexed by its README.
- Archived: `docs/PLAN.md` — the pre-Allostatik living document; historical detail beyond what the files above carry.
