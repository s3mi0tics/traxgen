# traxgen — Decisions

Locked decisions about how this project works — architectural choices, conventions, and resolved tradeoffs. Once something is locked here, don't renegotiate it without raising a checkpoint. This file is the project's reasoning trail: it records not just *what* was decided but *why*, so a later session doesn't relitigate settled ground.

This is the decisions layer, distinct from its `allostatik/` siblings — `plan.md` holds current operational state (what's in flight), `observations.md` holds process patterns (how the work tends to go). Point at those rather than restating them here.

**Update protocol.** At session close, append decisions locked during the session. If a locked decision later changes, mark it rather than deleting it — the trail matters:

- `[SUPERSEDED <date>]` — full overturn. Replace the Choice with the marker plus a pointer to the row that replaces it.
- `[AMENDED <date>]` / `[PARTIALLY SUPERSEDED <date>]` — partial change where the original mostly holds. Prefix the marker to the Choice; leave the original text intact.

Always name the superseding or amending row so the trail stays followable.

**Last updated:** 2026-08-26 (s28)

| Decision | Choice | Rationale |
|---|---|---|
| Language | Python 3.12 (pinned `.python-version`); TypeScript port after v1 | Ship v1 in one language; port when the pipeline is proven |
| Output format | Binary `.course`, schema `POWER_2022` (v4) | Matches the murmelbahn-documented format the app accepts |
| Target inventory | PRO Vertical Starter-Set (26832); Core (22410) cataloged alongside | Colby owns 26832 physically — generated courses can be validated against real pieces |
| Tooling | `uv` + `pytest` + `hypothesis` | uv for env; property-based tests earn their keep on hex math |
| Repo | `traxgen`, public, github.com/s3mi0tics/traxgen | — |
| License | Apache-2.0 | PLAN.md's table said MIT; the repo's LICENSE file says Apache-2.0. Confirmed Apache-2.0 during the 2026-07-22 migration — the LICENSE file is authoritative |
| Schema reference | murmelbahn Rust source (`lib/src/app/layer.rs`) | Reverse-engineered ground truth; imhex-schema mirrors it |
| Project layout | Flat package (`traxgen/traxgen/`), not src-layout | Simplicity; no packaging need for src indirection yet |
| Validator API | Soft `validate()` → `list[Violation]`; `validate_strict` raises | Generator needs inspectable violations; strict wrapper for tests/CLI |
| Rail data model | Flat `Mapping[RailKind, int]` + separate `straight_rail_limits` | Rail lengths are fixed (1/2/3 hexes), not cascading — per-length budgets required |
| Upload transport | Stdlib `urllib`, no `requests` | One endpoint doesn't justify a dependency |
| Upload headers | Hardcoded module constants matching the real iOS app | Only the full verified set is known to work (open unknown #12); cargo-culting is safe |
| Upload error model | Rich exception hierarchy per failure mode | Callers can distinguish transport vs server vs malformed-response; note upload endpoint doesn't validate payloads, so 4xx/5xx paths are defensive |
| Upload test strategy | In-process mock server default; live canary behind `network` marker | Fast deterministic suite; one real-world tripwire on demand |
| Correctness contract for serialization | Byte round-trip, NOT Python-float equality | f32 storage makes `-0.2` read back as `-0.2000000029…`; bytes are the promise, floats aren't |
| Minimal-course goal piece | `GOAL_RAIL` over `GOAL_BASIN` | Full root-placeable tile vs insert needing a host frame |
| Baseplate LayerKind | Both `BASE_LAYER` and `BASE_LAYER_PIECE` count as baseplates (b8052e4) | Rust doc-comment's plural "all base plates"; GDZJZA3J3T's 15 baseplates are all `BASE_LAYER_PIECE` |
| Cannon is not a starter (d89218f) | Modeled via `energy_profile.energy_input_j`, not `is_starter` | Requires an incoming ball; energy injector, not origin |
| `BASEPLATE_COVERAGE` dropped from v1 | Revisit in M5+ if baseplate shape becomes derivable | Original spec didn't match the domain model; re-interpretation blocked on open unknown #3 |
| `TILE_INDEX_COLLISION` dropped from v1 | Revisit if fixtures reveal a pattern | Real app courses violate it (non-root nodes default index=0); rule as specced false-positives |
| App integration path | Share-code upload API only; sideload is dead | iOS app registers no local data-in path (2026-04-22); upload endpoint verified end-to-end |
| Render verification | Automated harness + play-button oracle only; manual verification carries no evidentiary weight | Silent Mac→iPhone clipboard failure invalidated a session of manual observations (promoted from the pattern now in `observations.md`) |
| Migration classification (2026-07-22) | `docs/PLAN.md` = fold-in → archive; `.cursorrules` = live (code conventions, pointed at); root README = live; `docs/refs/` = live (Layer-3 corpus); CLAUDE.md = rewritten as adapter over `allostatik/` | One source of truth per domain; the required migrate-branch record |
| Tool rename: Allostat → Allostatik (2026-08-03) | [AMENDED 2026-08-04] [AMENDED 2026-08-07 — see the rename-reading row below] `allostat/` → `allostatik/` via migrate.py (folder, fences, imports, in-folder references); `scripts/update_docs_20260727.py` repointed the same day. Surviving "allostat" mentions (docs/refs, docs/PLAN.md archive banner, older prose) are historical and refer to what is now Allostatik. The historical-prose reading holds for `docs/refs` and the `docs/PLAN.md` banner, but not universally: three surviving mentions in CLAUDE.md were live path pointers into the renamed folder, leaving the orient step reading files that no longer existed for ~25 hours. Repointed at `4988c41`; see the 2026-08-03/04 session-log row in `plan.md`. | Upstream rename of the context tool. In-folder self-references were swept with the rename — rows here now read "allostatik" even where the recorded events happened under "Allostat" |
| Rename reading, narrowed (2026-08-07) | The 2026-08-03 row's "surviving mentions are historical" reading is **replaced** by a sharper one: *prose about the tool* is historical and stays; *path-shaped mentions* (`allostat/...`) are live pointers and get repointed wherever they appear. A repo-wide `grep "allostat\b"` on 2026-08-07 returned exactly three hits, all now accounted for: two are narrative inside `plan.md` and `decisions.md` (correctly historical), and `docs/PLAN.md`'s archive banner was a live pointer and was repointed. Seven further path-shaped hits in `docs/refs/README.md` and `docs/refs/android-automation.md` were repointed the same day. The question is now closed by exhaustive search rather than left to per-file judgement. | The original reading failed twice on the same distinction: three CLAUDE.md mentions (`4988c41`) and then seven in `docs/refs`. Two failures of one reading is the double-loop signal, so the rule changed rather than the instances being patched again. A single grep converted an open question into a closed one |
| Generator certification (2026-08-07) | `generate_minimal()`'s app-certified status is **verified**, not inherited: its bytes differ from `FLW4TMLP5V` only in the title string (`0e "traxgen-norail"` vs `0f "traxgen-minimal"`), and are byte-identical from the end of the title onward. Title is metadata and cannot affect validity, so the certified geometry stands | The claim had stood since `f6e5717` with nothing ever comparing it to the artifact. Surfaced because the byte-identical control returned a *new* share code instead of the certified one — which also confirms content-hash dedup rather than falsifying it |
| Measurement runs carry a control at both ends (2026-08-07) | Any sweep reading the app oracle renders a known-good geometry first *and* again after the last cell. A non-active closing bracket overrides the run's verdict to `HARNESS_SUSPECT` regardless of what the data said | An opening control proves the harness worked at render 1 and says nothing about render 25. Earned within the hour: the closing bracket caught a false `active` from a splash screen that would otherwise have been reported as `MODEL_WRONG` |
| The play-button oracle refuses ambiguous frames (2026-08-07) | `detect_play_button_state` raises `OracleFrameError` when `white_frac >= 0.50` rather than returning a verdict. Threshold calibrated on measured screencaps: splash 0.942, real renders 0.013–0.014 | The sample is a brightness test, not a validity test, and returned `active` for a loading screen. A false `inactive` loses data; a false `active` invents a finding — guard the direction that invents |
| Goal rotation is a goal-side rule (2026-08-08) | `g = (d + 1) % 6` maps the goal's direction from the starter to its required `hex_rotation`, and takes **no starter term**. Confirmed on every active cell ever measured — E→1, NW→3, NE→2 — across two exhaustively swept starter rotations, zero exceptions. The rival `g = (d + 1 + 3s) % 6` was rendered and is dead | The rule looked partial for a day because the 2026-08-07 sweep pinned the starter at 0 and so conflated two constraints. Separating them showed the rotation rule was never the restricted part. M5.c computes goal rotation from direction alone |
| The live direction set is a measured table, not a derived rule (2026-08-08) | [PARTIALLY SUPERSEDED 2026-08-21 — see "The live-direction set is a conjunction, and the table was missing a coordinate" below. Every row was measured with the starter pinned at local (0,0), a **plate corner**, and the table never recorded that coordinate; the set is not a starter-rotation property. What survives is the refusal to interpolate, now owed to *positions* as well as rotations.] [AMENDED 2026-08-10 — the table is now complete: all six starter rotations swept exhaustively, and the pattern is parity (even → {E rot 1, NW rot 3}, odd → {NE rot 2}). It stays a **table**, not a formula: parity is the measured shape, and the mechanism behind it — why parity, why sizes 2 and 1 — is still unexplained, so encoding it as a rule would again smuggle in the part that keeps being wrong. `live_directions()` still refuses rotations outside 0..5.] Which directions connect depends on the starter's `hex_rotation` in a way no symmetric model yet explains: s=0 gives two cells (E rot 1, NW rot 3), s=1 gives one (NE rot 2). Generators pick from the measured table and **do not interpolate** to unswept rotations | Every rotating-exit-set model proposed has been refuted by render, in both chiralities. A table of harness-verified geometries is honest about what is known; a fitted rule would smuggle in the part that keeps being wrong. See open unknown #15 |
| The sweep's positive control stays at starter rotation 0 (2026-08-08) | `sweep_goal_rotation.py --starter-rot N` sweeps every cell at N but pins the control at s=0, gives it kind `control_positive`, and excludes it from `classify()`'s per-direction tally | The app-certified geometry renders **inactive** at s=1, so a control that followed the swept rotation would abort every run but one, reporting `HARNESS_SUSPECT` for a healthy harness. Counting it as an observation of its own direction would invent an active NW cell at rotations where none exists |
| A model fitted to the record is tested by prediction, not by fit (2026-08-08) | When a model is built from prior data, the next run declares each cell's expected verdict **in code** before rendering, and the classifier compares outcome to prediction rather than deriving a mechanism from outcomes. `build_cells()` refuses to start if a declared prediction disagrees with the model's own function | Earned twice in one session. The parity model reproduced all 42 historical cells and still missed one of four predictions — fitting the record carries no evidence. And the starter sweep's free-text verdict asserted a mechanism the same run refuted: a pre-declared *trigger* and its *explanation* are separate claims, and only the trigger was tested |
| Connection knowledge is three-valued (2026-08-10) | `graph.py` classifies every starter/goal pair as `CONNECTED`, `DISCONNECTED`, or `UNMEASURED`, and `DISCONNECTED` is claimed **only inside the measured space** — at a swept starter rotation, where all 36 direction × rotation cells were rendered and a miss is a measured miss. Cross-layer pairs, out-of-range rotations, and anything else the record has not covered are `UNMEASURED`, never "no" | A two-valued API forces every unknown into a false claim, and the false claim it would pick ("not connected") is indistinguishable from a measured negative. The third value is what keeps the empirical discipline legible in code rather than only in prose |
| `START_GOAL_CONNECTED` severities mirror the epistemics (2026-08-10) | ERROR only when every starter/goal pair is *measured* disconnected; WARNING when the best evidence is `UNMEASURED`; silent when the course carries explicit rails (railed connection paths are unmodeled — open unknowns #2, #7) or has no starter/goal at all (that is `MISSING_STARTER_OR_GOAL`'s finding) | A validator that errors on unmeasured configurations converts a gap in the record into a false claim about the course, which is the same failure the frame guard exists to prevent one layer down (observations #17). One rule, one domain: absence and connection stay separate findings |
| The sweep queue stops on anything that questions the harness (2026-08-10) | `run_sweep_queue.py` **continues** past a mechanical hole — `INCOMPLETE` with no abort earns exactly one auto-resume, then the gap is recorded and the next rotation runs anyway — and past `MODEL_WRONG`. It **stops** the whole campaign on any abort (`harness`/`model`/`budget`), on `HARNESS_SUSPECT`, on a sweep that dies without writing results, and on any unmodeled exception | The gate moved from per-command to per-plan, not away. A hole in s=3 says nothing about s=4, and `MODEL_WRONG` questions the model while leaving later rotations honest measurements — both are safe to continue. Every stop condition instead implicates the *shared* harness or conditions, so continuing would spend renders on measurements no human has decided to trust |
| Containerizing the render loop is rejected for this Mac (2026-08-10) | Sweeps keep running from Colby's shell. The Android emulator needs hardware virtualization that Docker's Linux VM on an M1 does not expose; containerizing only the build/upload half would sandbox the safest part while `adb` — the part that touches something real — stays outside. Containment today comes from what already exists: the cloud sandbox for agent shell work, the Filesystem MCP allowlist as the standing grant, the emulator as its own sandbox, and git as the undo. **Trigger for revisiting:** a Linux host with KVM, at which point one throwaway container per run is the right architecture | Raised by Colby as a security measure ("make sure I don't accidentally give the wrong permissions"), which is a containment question distinct from the MAPE-K approval gate — both are wanted. But on this hardware the container buys isolation at the cost of the render engine, and the reviewed-script-plus-least-privilege story is stronger than the isolation it would trade away |
| Port records store the observation, not the interpretation (2026-08-18) | A port observation persists (tile kind, raw `side_hex_rot`, the tile's `hex_rotation`, integer height level with the raw `exit_local_pos_y`, rail kind, retainer kind, world position, source course, evidence channel). The tile-relative edge is **derived at read time and never stored** | Storing rel would bake the tile-intrinsic hypothesis into every record, including the ones that contradict it. Validated at scale the day it was locked: raw+rotation records let the parity-class structure *emerge* on derivation — 26 kinds collapse to one parity class, and the spread class shows bimodal structure a stored-rel record would have flattened. Same discipline as the live-direction table staying a table |
| The live-direction set is a conjunction, and the table was missing a coordinate (2026-08-21) | Which directions connect is `live(s, pos) = plate_available(pos) ∩ {(r + s) % 6 for r in STARTER_PORTS}`, with `STARTER_PORTS = {0,2,4}` (corpus, n=380 unambiguous) and `plate_available` read off the BASE_LAYER_PIECE footprint (corpus, n=28,494). Local (0,0) is a plate **corner** — its W/SW/SE neighbours appear zero times in the corpus — so three of every 36-cell sweep's directions were off-plate and dead by construction. That, not the starter, is the 2-vs-1 asymmetry. Consequence for code: any connection claim is keyed on the course's baseplate layout as well as the starter's rotation, and `graph.py` currently is not — see `plan.md` | Tested by prediction, not fit (the 2026-08-08 lock). Edge run at (0,1) refuted the port-only model — off-plate cells the ports allowed stayed dark. Interior run at (-3,2) refuted `graph.py`'s own position-blind table — SW rendered **active** after being dark in all six exhaustive corner sweeps. 14 cells across two starter positions never previously rendered, every verdict declared in code beforehand, zero misses, harness bracketed at both ends of both runs. Both inputs were corpus-measured before any render, so the model was not fitted to the thing it predicted |
| Every render run carries a control at the position under test (2026-08-21) | The 2026-08-07 both-ends control proves the *harness* works. It does not prove the *geometry family* under test is renderable at all. So a run that moves a tile to new coordinates also renders a cell every rival model calls active at those coordinates, and a dark one aborts the run as `SETUP_SUSPECT` rather than yielding a verdict | Without it, "the starter cannot sit here at all" and "the model is right" produce the identical all-inactive run — a null that discriminates nothing (observation #21). The plate-membership probe predicted its two discriminating cells dark, so an unrenderable starter cell would have produced a flawless-looking confirmation of the very hypothesis under test. The local control is what made the result mean something |
| Ask what the corpus can *see* before designing a render experiment (2026-08-21) | The corpus answers questions about what the format **serializes** — which cells a layer occupies, tile kinds and rotations, rail exit identifiers and their heights. For those it is free, fast, and returns a distribution rather than a point, so it goes first and renders confirm. The rule is a **visibility test, not an ordering**: establish that the format records the thing before reading anything into the result, because on anything it does not record, **zero observations means invisible, not absent** | Both halves were demonstrated the same day, which is why the rule is scoped this way rather than as "ask the corpus first". Open unknown #3 (baseplate shape, open since April, blocking `BASEPLATE_COVERAGE`) fell to one probe in minutes and supplied the missing term in #15; the render channel then spent 14 cells confirming what the corpus proposed, against the ~311k a pairwise sweep would have cost. But the session's *planned* experiment died on the other half: GOAL_RAIL has **0** port records in 43,375, because it carries its own rail and so never appears in a `RailConstructionExitIdentifier` — no quantity of further courses changes that. An unscoped "ask the corpus first" would have read that zero as evidence about goal rails. Renders remain the oracle of record; they are not the instrument of first resort *for questions the format answers* |
| Claims and predictions are separate surfaces in `graph.py` (2026-08-21, s22) | `connection_status` / `classify_pair` answer only from `MEASURED_RUNS` — every rendered campaign, each recording whether it swept goal rotations — and return `UNMEASURED` everywhere else, *including configurations the conjunction confidently predicts*. The model lives on `predicted_live_directions` / `predict_connection`, for generators to propose with. The corner table survives as a regression fixture the conjunction must reproduce (every campaign in `MEASURED_RUNS`, no free parameters, swept as a class rather than as named asserts — stated without a count so adding a run cannot make this sentence false, which it did on 2026-08-23). `connection_status` takes `layer_kind` and `starter_local_pos` as required keyword-only arguments with **no defaults**. Multi-plate courses classify `UNMEASURED` until #17/#16 decide how membership composes | A model that went 14-for-14 is still a model (observations #20); folding it into the claim surface would make generator proposals indistinguishable from harness findings. A default position would silently restore the exact s21 defect — an answer measured at the corner, applied everywhere. Extends the 2026-08-08 table row and the 2026-08-21 conjunction row |
| A historical claim kept executable is a frozen quotation (2026-08-21, s22) | When a refuted claim stays in code as a rival to race (the probe's `table` model), it is stated as its own provenance-commented literal (`TABLE_CLAIM_2026_08_10`), never derived from the live record it once coincided with, and its transcription is guarded literal-vs-literal. Locked after a five-agent panel: repointing at corrected code is **circular** — `MEASURED_RUNS` now carries the probe's own renders, so a rival that asks it is graded against its own answer key (verified by enacting the repoint: 13 loud failures, and the silent trap is the fixture-update edit after) — and **retiring** it kills the next planned run, where `table` is the only rival that separates anything at interior rot 1 | Quotation vs pointer: DRY points at live shared facts, but a quotation's reason-to-change is *never*, so deriving it from the live record is false DRY — the history would rewrite itself when the library corrects. The panel also proved the guard necessary: the prior test compared the rival to the very mapping it read and stayed green under record corruption (#12's shape, mutation-proven) |
| The course corpus lives outside the repo (2026-08-18) | `~/Claude/Projects/traxgen-corpus/` holds the harvested `.course` binaries plus `buildable_courses.json`; the binaries are **never committed**. The fetch/probe/mine scripts and the code list are committable — they make the corpus refetchable | The 808 files are other people's shared courses; republishing them wholesale in a public repo is a different act from committing three attributed test fixtures. Reproducibility comes from committed scripts pointing at the source (DRY), not from duplicating the corpus into git |
| The harness verifies the foreground, and raises rather than repairing (2026-08-23, s23) | `render_course` calls `assert_app_in_foreground` before the first tap and again immediately after the frame is captured, and **raises** (`WrongForegroundAppError`) when another app is in front. It does not launch the app itself. `reset_first` is a separate opt-in that **establishes** main-menu state by force-stop-and-relaunch; it never claims to verify it, because splash and main menu are byte-identical to `dumpsys window` (two committed captures, identity asserted by a test). Scope is stated in the module rather than implied: this closes the s21 launcher failure and is blind to the 2026-08-07 splash failure. `AdbContext` carries injected `runner` and `sleep` seams, both defaulting to the real thing | A guard that silently relaunches makes the precondition invisible again, which is how it was lost in the first place — `assert_emulator_ready` was read as meaning more than it checks, and that gap returned a confident `inactive` off the launcher (observations #17). Colby's call. Reproduced against pre-guard `8518b79` before the fix was trusted: launcher in front plus a dark frame yields `validity='inactive'`, because the near-white frame guard passes dark wallpaper at `white_frac 0.000` |
| Transient upload failures are retried by exception class, and the retries are counted (2026-08-23, s23) | `upload_course_with_retry` lives in `traxgen/uploader.py` — 3 attempts, 2s/6s backoff — and retries **only** `UploadServerError` (5xx) and `UploadNetworkError` (DNS/TLS/timeout). A 4xx is not retried, because the payload is wrong and retrying is a slower no; a malformed response is not retried, because it has never fired and a policy for it would be invention. The attempt count is returned, not swallowed, and the probe records it into the sidecar | The endpoint's flakiness is a *designed-for* condition (the 2026-08-10 queue lock), but only the queue implemented it — a standalone probe lost a whole campaign to two timeouts that took out both controls. Counting matters as much as recovering: the failure rate is itself the measurement behind observations #15, and a run that quietly recovered twice must not read as a clean one. Content-hash dedup makes a retry safe — an upload that did land returns the same share code |
| A course with more than one baseplate is `UNMEASURED` at the claim surface (2026-08-24, s24) | [AMENDED 2026-08-24, s25 — the *verdict* stands and the *mechanism* is replaced: the baseplate count is gone, and multi-plate courses reach `UNMEASURED` by missing the record instead. See "The record keys on the plate layout, and the guard is a lookup rather than a count" below, and the two rows after it. The count also carried a hole this row's own text shows — it counts `BASE_LAYER_PIECE`, while `b8052e4` counts `BASE_LAYER` too.] `start_goal_status` returns `UNMEASURED` for any course carrying more than one `BASE_LAYER_PIECE`, whatever its pairs say. Note the level: `classify_pair` sends only **cross-layer** pairs there, so a same-plate pair on a four-plate course was answered from the single-plate record. Every row in `MEASURED_RUNS` was rendered through `build_variant`, which emits exactly one layer, so "one plate at world (0,0)" is an unrecorded precondition on all nine campaigns | Unreachable until `traxgen/layout.py` made multi-plate courses buildable, and reachable it produced a **false ERROR** through `START_GOAL_CONNECTED` on the exact arm the add-a-plate probe exists to render — asserting one reading of #17 as a harness finding. That is the s21 defect one level up (an answer measured in one configuration, applied to another) and what the 2026-08-10 severity lock forbids. Found by the adversarial panel, not by the author; reproduced against pre-guard code before the fix was trusted. The more principled version — making the plate set a required term on `connection_status`, as s22 did for `starter_local_pos` — is sequenced, not done |
| `layout.py` is a second write path, deliberately (2026-08-24, s24) | `traxgen/layout.py` builds a course from an explicit plate layout. `generator.py` is **not** rebased on it, and a single-plate layout is pinned byte-identical to `generate_minimal()` by test. `build_variant` delegating to it is sequenced and recommended; `generate_minimal()` delegating is refused | Enacted by the panel and refuted: with `generate_minimal()` rebased on `build_course`, mutating `build_course`'s own `guid=0` — the value the app was tested against — leaves the whole suite green, because the byte-identity test becomes a function compared to itself (#12's shape). `generate_minimal()` is the frozen quotation of FLW4TMLP5V; `build_course` is an independent transcription. Collapsing them is false DRY, the same quotation-vs-pointer distinction locked in s22 |
| Multi-plate courses keep the certified layer height, not the corpus's (2026-08-24, s24) | `layout.CERTIFIED_LAYER_HEIGHT = 0.0`, which is `generate_minimal()`'s value and FLW4TMLP5V's. The corpus disagrees — 4,598 of 4,599 real `BASE_LAYER_PIECE` layers carry **-0.2** and exactly one carries 0.0 — and the parameter exists so a run can use -0.2 deliberately | One variable, not two: a multi-plate course at 0.0 differs from the certified course in **plate count alone**, so a dark render cannot be blamed on the height. The steelman for -0.2 (replicate what real courses do) dies on a fact the module already cites: FLW4TMLP5V *is* a `BASE_LAYER_PIECE` at 0.0 carrying a STARTER and a GOAL_RAIL, and it rendered active, so 0.0 is proven non-disqualifying. -0.2 is the first thing to try if the local control goes dark. Note the f32 contract bites at -0.2: bytes round-trip, object equality does not |
| The #17 experiment is a 2×2, not an add-a-plate render (2026-08-24, s24) | The same **world cell** is rendered under both addressings on one four-plate course: arm 1 as an in-window local on the plate that owns it, arm 2 as the out-of-window local on the home plate with real plate now beneath it. Plus the certified control at both ends and a local control at the position under test | The handoff's design — "add a plate beneath a dead cell; if it lights, off-plate is physical" — changes both factors at once, so a positive result discriminates nothing (#21 in the mirror). s21 measured the third cell of the 2×2 already (no plate, out-of-window: dark); the fourth (no plate, in-window) is impossible by construction, since in-window *means* on the footprint. Arm 1 vs arm 2 isolates addressing; arm 2 vs s21 isolates plate presence |
| The odd-rotation interior run: the conjunction's positive half is measured (2026-08-23, s23) | At (-3,2) rotation 1, `{NE, W, SE}` all render **active**; E, NW, SW dark. 7/7 predicted, declared in code before rendering, bracketed by an active certified control at both ends plus a local control at the position under test. The frozen `TABLE_CLAIM_2026_08_10` rival is refuted on two cells simultaneously and its purpose is discharged. `plate` and `port_only` are *not* separated here — nothing is off-plate to separate them — so the pair of probes composes: the 2026-08-21 edge run kills `port_only`, this kills `table`, and `plate` alone survives both | The edge run established "off-plate kills" with both discriminators off-plate, leaving "on-plate plus port parity suffices" resting on one cell. This is that half. It also retires the standing caveat that every odd-rotation row sat at the corner, where three of six neighbours are off-plate — "odd gives one live direction" was a corner fact being read as a rotation fact. Recorded as the ninth `MEASURED_RUN`; the class sweep confirms the conjunction reproduces all nine with no free parameters |
| What a baseplate is has one home (2026-08-24, s25) | `plates.BASEPLATE_LAYER_KINDS` — both `BASE_LAYER` and `BASE_LAYER_PIECE`, the `b8052e4` classification — is defined once and imported by `validator.py` and `graph.py`. Neither redefines it | The validator held the only copy and `graph.py` could not import it (the validator imports `graph`, so the dependency would be circular), so `graph.py` had grown its own narrower copy counting `BASE_LAYER_PIECE` alone — and that copy was gating connection claims. Enacted before the move rather than argued: the certified course plus one empty `BASE_LAYER` returned CONNECTED from a record measured on one plate. `BASE_LAYER` appears in **zero** of 640 parsed corpus courses (`scripts/probe_plate_arrangement.py`), so the hole was latent, which is a reason to record it rather than a reason to leave two definitions disagreeing. Note the direction of the move: "which `LayerKind`s are baseplates" is a per-`LayerKind` fact and belongs beside the footprints; "how several plates sit together" (`STANDARD_SQUARE`) is not, and its move out of `plates.py` is still sequenced |
| The record keys on the plate layout, and the guard is a lookup rather than a count (2026-08-24, s25) | `MeasuredRun.plate_offsets` records where every baseplate sat in the course that was rendered, and `measured_run` treats it as part of the key alongside `layer_kind`, `starter_local_pos` and `starter_rot` — required keyword-only, no default, like the two s22 terms. `start_goal_status` no longer counts baseplates: a multi-plate course misses the record and every pair comes back `UNMEASURED` on its own. The verdict for multi-plate courses is unchanged; what changed is that it is now a consequence rather than a rule | s24's count was the blunt version of exactly this, and the `plan.md` item asked for the principled one — the same move s22 made when `starter_local_pos` became required with no default: a term the configuration silently supplied becomes part of the key. Removing the count also removed a kind-specific hole it carried (see the row above). A count cannot distinguish *which* plates from *how many*, which the record needs and s24's guard could not express |
| The layout is keyed relative to the starter's own plate, not in absolute world coordinates (2026-08-24, s25) | `plate_offsets_from(plate_positions, starter)` rebases every plate against the starter's layer position, sorted; `MeasuredRun.plate_offsets` stores that form. Absolute world positions were the first implementation and are refused | Absolute keying is the literal reading of "record what the campaign ran on" and it silently destroys a test the suite depends on. The s22 moved-board fixture translates the certified interior geometry so a world/local frame confusion lands the starter on the measured-dead corner (observations #26); under absolute keying the honest reading *and* that bug both return `UNMEASURED`, so the fixture stops discriminating and the blind spot s22 closed reopens with nothing failing. Verified by enacting absolute keying (the fixture fails) and then re-injecting the s22 confusion on top of it (the fixture fails identically). What the choice makes **explicit** rather than introduces: the absolute world offset of a course does not affect connection. No render has tested that — every campaign sits at world (0,0) — but the shipped model has always claimed it, since `connection_status` never sees a world coordinate. It is now named in code and pinned by a test instead of assumed |
| A sorted tuple, not a set: two plates at one position stay two plates (2026-08-24, s25) | The layout is `tuple[tuple[int, int], ...]`, canonicalised by sorting, rather than a `frozenset` | Sorting makes order not a degree of freedom a consumer can depend on untested (#30's defect, one module over). A tuple rather than a set keeps multiplicity: `layout.build_course` refuses to emit two plates at one world position and the corpus has never contained one — zero footprint collisions across 640 courses, which two coincident plates could not survive — but the **parser** will read one, and a set would fold the pair into a single entry and hand that course the single-plate record's answer. Enacted: with a set, the degenerate course flips `UNMEASURED` → `CONNECTED` |
| The record is consulted before any verdict, including the non-adjacency one (2026-08-24, s25) | `classify_pair` looks up the covering `MeasuredRun` **before** the `direction is None` short-circuit, and returns `UNMEASURED` when there is none. Only inside a covering run does a non-adjacent pair get `DISCONNECTED` | Learned by shipping the opposite for the length of one session. `DISCONNECTED`-from-non-adjacency rests on the *mechanism* (a rail-free connection is tile adjacency at `rail_count = 0`), which makes it feel safe to answer early — but it is a claim like any other. s24's baseplate count sat **above** that short-circuit and so covered every pair; replacing the count with a lookup inside `connection_status` moved the guard **below** it, and every non-adjacent pair on a multi-plate course was claimed measured-disconnected at ERROR severity. 599 of 640 corpus courses are multi-plate, so that was the common shape. The suite stayed green because the one multi-plate fixture used an *adjacent* goal — the single arm the defect does not touch. Found by an adversarial panel, independently by two of three lenses (observations #19, eighth firing); the fix was enacted by the panel and re-verified by mutation. General form: **the level a guard sits at is part of the guard**, and moving a check inward past an early return silently narrows it |
| Pixel stability is a third guard on a third axis, and it is necessary rather than sufficient (2026-08-24, s26) | `android.wait_for_stable_frame` polls screencaps until N consecutive frames differ by at most a tolerance, and returns the `FrameStability` predicate so the caller can record what it observed. Its scope is stated in the module rather than implied: **"the picture stopped moving" is not "the course loaded"** — a splash screen is perfectly still. The three guards answer three different questions: `assert_app_in_foreground` which **application**, `frame_white_fraction` whether the frame is a near-white **splash**, `FrameStability` whether the surface has stopped **animating**. What none of them sees is named too: a still, non-near-white, GraviTrax-owned screen that is not the render. Comparison uses `Image.Resampling.BOX`, not Pillow's default | The `uiautomator` channel cannot see the Unity surface at all, so pixels are the only route (the s23 scope correction). Stating the scope is the s23 foreground-guard lesson applied before the failure instead of after: a guard read as meaning more than it checks is how the precondition got lost the first time. The filter is not taste — measured on a 16x12 frame downscaled to 4x3, the honest area-average of a one-row change is 21.33 and BICUBIC (the silent default) reports 16.67 while NEAREST reports **0.000**; every filter but BOX attenuates motion, and an attenuated difference slips under the tolerance, i.e. a moving screen called still, which is the direction that invents data (observations #17). The same default is latent in `frame_white_fraction` and was **named rather than fixed**, because its 0.942-vs-0.014 calibration was measured through BICUBIC |
| The stability tolerance, sample count and downscale are declared, not measured (2026-08-24, s26) | `FRAME_STABILITY_TOLERANCE = 0.0`, `FRAME_STABILITY_REQUIRED_SAMPLES = 3`, `FRAME_STABILITY_DOWNSCALE = 4` are pre-declarations at the strict end, and `FrameStability.differences` retains every delta observed so the first live run reports the real distribution and the constants get set from it | No frame-to-frame delta of a real loaded GraviTrax course has ever been recorded, so the noise floor of a "still" Unity surface is unknown — temporal antialiasing and dithering both jitter. Picking a plausible number and letting it survive because nobody looked is exactly observations #18, so the predicate is also the instrument that calibrates itself, the route `scripts/calibrate_frame_guard.py` took for the frame guard. Strictness is the right side to start on (#17): a premature "stable" renders a loading screen and can invent a verdict, while a timeout costs a re-run and invents nothing. What the tests can and cannot say is stated precisely in `frame_fingerprint`'s docstring — changing the downscale *does* fail the suite, but that is change-detection, not validation |
| No `git` from the bridge VM at all, reads included (2026-08-25, s27) | Run no `git` command from the device VM; hand a terminal command to the Mac instead. `environment.md`'s fifth-bridge note said reads were fine and that is false: `git status` alone strands `.git/index.lock`, because it refreshes the index and the VM cannot unlink the lock afterwards. | The real split is *touches the index* versus not, and Colby's ruling is the blunt version rather than that edge, because the failure is silent — a stranded lock is invisible until the next `git` write on the Mac fails, so a human pays for the subtlety at the wrong moment. Measured: no `git add` was run in the session that found it. Observations #28's shape again |
| The record says which directions a run probed (2026-08-25, s27) | `MeasuredRun.directions_probed`, required with no default; `connection_status` returns `UNMEASURED` for a direction outside it, gated **above** the rotation check. `__post_init__` refuses `live_directions` outside `directions_probed` | `goal_rotations_swept` records rotation coverage *within* a direction and nothing recorded coverage *across* directions. It moved no verdict — every campaign to date probed all six, verified by running `probe_plate_membership.build_cells` for all three probe configurations rather than reading their provenance prose. Its purpose is the run that has not happened: a single-cell render on an unmeasured layout would otherwise have to be recorded as a six-direction result, asserting five verdicts it never measured. The gate sits above the rotation check because a direction never rendered has no rotation of it measured either — the level a guard sits at is part of the guard (s25) |
| Where the goal stood is a key term, not a refusal (2026-08-25, s27) | `MeasuredRun.goal_layer_kind` and `goal_plate_offset`, both required, no default. `classify_pair`'s cross-layer refusal is gone; a goal on another plate or a non-baseplate layer misses the record instead. `goal_plate_offset` is `tuple \| None` — `None` means the goal stood on the starter's own layer | Same move as s25 for the plate set and s22 for the starter position: a term the configuration silently supplied becomes part of the key, and the verdict for an unmeasured shape becomes a consequence rather than a rule. This is what made #17's arm 1 recordable, and the 2x2 then measured it **active** — so the field existed before the result that needed it, which was the whole point of building it first. Two corrections came from enacting: (a) the offset is a sum type because two distinct layers can share a world position, so `(0,0)` cannot mean both "same layer" and "a different layer that coincides" — refuted by an existing test the moment it was built; (b) `goal_layer_kind` was decoration until a mutation deleting it from the key left 679 tests green — the configuration that separates the terms is precisely the one arm 1 creates, and a test now builds it |
| The 2x2 runs on two target cells, not one (2026-08-25, s27) | Two directions qualify at starter local (0,1) rot 0 — E and SW are both port-allowed, both off the home footprint, and both covered by the completing plate at `(5,0)`. Both are rendered, so "five renders including the controls" becomes seven. `classify` treats the targets disagreeing as its own verdict | Found by the derivation's own guard refusing the first version, which asserted exactly one. A replication rather than a design change, and it earned both renders: this project has twice had one direction behave unlike its neighbours — SW rendered active at an interior cell after six exhaustive sweeps called it dark — so a single-direction result would have been n=1 exactly where the record has been surprised before. **Vindicated by the run**: E and SW agreed on both arms, which is what makes `ADDRESSING_MATTERS` a replicated finding rather than one cell's behaviour |
| "Off-plate" is a coordinate window, not a physical absence (2026-08-25, s27) | A tile's `local_hex_position` must lie inside **its own layer's** measured footprint. A cell addressed out-of-window does not connect **whether or not** real baseplate sits under that world cell; a cell addressed in-window on the plate that owns it **does** connect, across a plate boundary. So the generator addresses every tile on its owning plate (`layout.owning_plate` is the primitive), and `world_hex_position` plus an in-window local is how a course reaches a neighbouring plate. Open unknown #17 is answered on the addressing axis | The 2x2, 7/7, both certified controls active and a local control at the position under test, replicated on two independent directions (E and SW). Arm 1 (in-window on the owning plate) **active** both times; arm 2 (same world cell, out-of-window local on the home plate, completer plate really underneath) **dark** both times. Arm 1 vs arm 2 isolates addressing; arm 2 vs the s21 lone-plate run isolates plate presence, and both being dark says plate presence alone rescues nothing. **The shipped model was refuted by prediction, as the probe's docstring declared it would be** — `predict_connection` carries no plate-set term, answered from the starter's own footprint, and called both arm 1s inactive (observations #20). **Scope, stated because in-window on your own plate always implies support by that plate:** this run cannot say whether physical support is an *additional* term. The half-holes (s26) are the only known configuration where window and support come apart, which is why that experiment is now sequenced ahead of folding a replacement term into `predict_connection` |
| The harness refuses frames that match a known dead screen (2026-08-25, s27) | `match_refused_screen` compares every captured frame against a set of reference screens shipped in `traxgen/data/refused_screens/` and `render_course` raises `RefusedScreenError` — **above** the oracle, because its answer on such a frame is well-formed and wrong, and **below** cleanup, because raising first leaves a course open and turns one bad render into a cascade. `probe_plate_boundary` retries once, by exception class, recording attempt counts and refusal distances into the sidecar. Threshold `REFUSED_SCREEN_DISTANCE = 10.0`, declared inside a measured gap | The fourth axis, and the gap `android.py` had already named as uncovered: a still, non-near-white, GraviTrax-owned screen that is not the render. Two of seven renders in the first 2x2 attempt landed on the app's build tutorial and the oracle read the greyed play button of a course that never loaded. Measured at fixture geometry 150x67: **2.262** between the two dead frames against **25.890–26.585** to three real renders, 11.4x, and the ratio holds from 1200x540 down to 100x45; the comparison frames are committed so the table is re-runnable (observations #24). Erring high is the right side (#17) — too high costs a re-run, too low hands a dead screen to the oracle. **The resampling filter is load-bearing**, found by mutation: `NEAREST` puts the dead frame at 10.298, above the threshold, so the careless default would miss the exact frame the guard was built from. **Scope, stated rather than implied: this is a SIGNATURE guard, not a mode guard.** It recognises known dead screens; an unknown one passes it exactly as it passed the other three. Closing the mode needs a positive test ("this frame contains a course") and none exists |
| Physical support does not gate connection; addressing is the whole rule (2026-08-26, s28) | A tile connects when its `local_hex_position` is in-window **on the layer it is addressed on**, and nothing else. Physical completeness of the cell is **not** a term: a goal on a half-hole -- addressable, in-window, and missing half its hole -- connects on a lone plate with no completing neighbour. Open unknown #17 is now answered on both axes (addressing, s27; support, s28). Note the scope split this creates and does not resolve: the app **editor** still refuses to place a piece on an uncompleted half-hole (`decisions.md`, s26), so *connection* and *buildability* are different questions with different rules, and the generator owes the user both | The half-hole campaign, `WINDOW_ONLY` twice -- nine arms in 589.3s, then the same design re-run with a negative control added, ten arms in 651.4s. Goals on both tested half-holes -- `(-2,5)` E and `(-1,3)` SW from starter `(-2,4)` rot 0 -- rendered **active on a lone plate**. The **predicted null held on both directions**: adding a real lattice neighbour at `(-5,0)`, which completes a disjoint seam set and leaves these two as unsupported as the lone arm, changed nothing (`lone` == `null` == active), so the one-plate-vs-two confound is excluded by measurement rather than by argument. Both certified controls active (`KN6F459ZR3`), the solid local control active, the **negative control** (the same tested cell at a wrong goal rotation) `inactive` on the same boot, no retries and no refused screens in either campaign, and every non-control arm a distinct share code (seven of seven in the nine-arm run, eight of eight in the ten-arm), so nothing deduped. The completer/null split was chosen by **hole-adjacency**, not by `_is_gapless` -- that test calls both `(5,0)` and `(-5,0)` gapless, because gaplessness of a union is symmetric while completion of a *particular* seam is not, and picking the null with it would have produced two arms that differ in nothing |
| The model's plate term is *where the goal is addressed*, not *which plates exist* (2026-08-26, s28) | `predicted_live_directions` and `predict_connection` take `goal_plate_offset`, **required with no default** -- `None` for the starter's own plate, a coordinate for another. A direction is live when the neighbouring cell is in-window on **that** plate, intersected with the starter's ports. No support term. Reduces to the pre-2x2 single-plate model exactly when the goal is on the starter's own plate, swept as a property over every footprint cell and all six rotations | **The looser reading was written first and the record refuted it before it shipped**, which is the part worth keeping. "Does *some* plate in the layout cover the cell" is the obvious replacement term, and the whole-record sweep failed on the 2x2's **arm 2** -- same starter, same layout, same direction as arm 1, and dark. So the live set is not a function of (starter, layout) at all. The corrected term reproduces **all eleven rows with no free parameters**, verified by running the candidate over `MEASURED_RUNS` *before* the patch was written rather than after (observations #20's discipline, applied to a model correction). Required-no-default for the third time (s22's `starter_local_pos`, s25's `plate_offsets`, now this): a default silently restores the refuted model on exactly the courses where it is wrong. Mutation battery 7 of 7 with the control caught, including both "plate term dropped" and "port term dropped" |
| A half-hole is not a usable square until a neighbouring plate completes it (2026-08-24, s26) | Three cells per `BASE_LAYER_PIECE` — local `(0,1)`, `(-1,3)`, `(-2,5)` in `(y,x)` — have their centres exactly on the plate boundary, so the app editor **refuses a piece there** unless the plate at delta `(5,0)` supplies the other half. A lone plate has 30 addressable cells and 27 usable ones. The generator must not place on those three without the completing plate in the layout. The cells are **derived** from `plates.MEASURED_FOOTPRINTS` by `scripts/probe_plate_seams.py`, never typed | Colby confirmed the refusal in the editor, which is a channel neither the corpus nor the render oracle can supply: the corpus shows what people *use*, the oracle shows whether a built course *loads*, and only the editor shows what the app *allows*. Checked rather than taken: across 640 corpus courses and 28,494 placements, **1** tile sits on an uncompleted half-hole against **2050** on completed ones, with a control (670 tiles on *whole* holes at the same edge of uncompleted plates) proving edge cells are used at all — without that control a zero would have measured nothing (observations #21). The one exception is unexplained and n=1, so it is recorded rather than theorised. Note the epistemic scope, same shape as the corpus-visibility lock: **editor rules are not format rules**, so this proposes and does not discharge #17 — arm 2 of the 2×2 is deliberately a course the editor would never build |
