# traxgen — Plan

This file holds the project's **operational state**: where things stand, what's in flight, where it's going, and in what order it gets there. The architecture — what a layer is, how the pieces compose — lives in `CLAUDE.md` and the README. The project's purpose and direction live in `project-instructions.md` and `vision.md`. This file is the live state in between.

---

## Living document discipline

Keep this file current at session close, as part of the close routine in `workflow.md` — that routine owns the *how*; this is just what to touch here:

- **Status changed?** Update Current state.
- **Item completed?** Remove it from Sequenced work in flight.
- **New work surfaced?** Add it to Sequenced work, naming its sequencing strategy.
- **Session done?** Append an entry to the Session log.

This file is **operational state** only. Don't duplicate `decisions.md` (locked choices) or `observations.md` (recurring process patterns) — point at them, and let each be the source of truth for its own domain.

## Current state

Phase 1 (single-track pipeline proof), late-stage. Milestone status:

| Milestone | Status |
|---|---|
| M1 Foundation (types, hex, inventory) | done |
| M2 Domain + parser | done |
| M3 Round-trip (byte-compare) | done (bbf7e36) |
| M4 Validator | done — 12/12 v1 rules; 3 Phase-2 rules deferred, 2 dropped. **`START_GOAL_CONNECTED` added 2026-08-10** as the 13th registered check, unblocked by M5.c |
| M5 Generator | M5.b-minimal done (2d89e77); M5.b-fix done (f6e5717) — `generate_minimal()` emits the validated no-rail adjacency shape, app-certified via `FLW4TMLP5V`. **M5.c done 2026-08-10** — `traxgen/graph.py` holds the goal-side rotation rule, the complete six-row live-direction table, and three-valued `ConnectionStatus`. **M5.a partially done** — `PlacedTile` + world-coordinate iteration + pair classification exist; no traversal, no multi-hop path yet. **s22 2026-08-21: `graph.py` is plate-aware** — claims answer from `MEASURED_RUNS` only, the conjunction model lives on its own `predict_*` surface, and new `traxgen/plates.py` owns the footprints |
| M6.a Upload | done — `uploader.py`, mock-server tests + live canary |
| M6.b Round-trip verification | **done 2026-08-08** — connection semantics resolved. `g = (d + 1) % 6` is a *goal-side* rule, confirmed on every active cell ever measured with zero exceptions. What varies is the *live direction set*, which moves with the starter's `hex_rotation`: exhaustively mapped at s=0 (E rot 1, NW rot 3) and s=1 (NE rot 2 only). Characterizing s=2..5 is open unknown #15, not a blocker. **2026-08-21: the "moves with the starter's `hex_rotation`" reading is superseded** — the live set is a conjunction of the starter's port parity and which neighbouring cells are on the baseplate; see answered #15 |
| M6.c Automated render verification | done — Android harness + play-button validity oracle, ~25s/code |

First traxgen-generated course certified valid by the app: share code `FLW4TMLP5V` (no-rail STARTER + GOAL_RAIL adjacency shape). The validated geometry: STARTER@(0,0) rot 0 + GOAL_RAIL@(-1,0) rot 3, `rail_count = 0`.

**2026-08-21 — connection is understood, and shipped code has not caught up.** `live(s, pos) = plate_available(pos) ∩ starter_world_ports(s)` explains every cell ever rendered, and both of its terms were read off the corpus before any render (answered #15, answered #3). Two forward-predicted runs at starter positions no sweep had used — (0,1) and (-3,2) — landed 14 of 14, and SW rendered **active** at an interior cell after being dark in all six exhaustive corner sweeps. The immediate consequence was a defect: `graph.py` keyed connection on starter rotation alone and claimed `DISCONNECTED` — at ERROR severity through `START_GOAL_CONNECTED` — for valid courses whose starter was not on the plate corner. **s22 (same day) closed the gap.** `traxgen/plates.py` owns the per-`LayerKind` footprints (generated from the corpus, never typed: 30/19/7 cells), and `graph.py` composes the conjunction with claims and predictions on separate surfaces — `connection_status` answers only from `MEASURED_RUNS` (the rendered campaigns, each recording whether it swept goal rotations) and returns `UNMEASURED` everywhere else, while `predict_*` exposes the model for generators to propose with. The false ERROR is gone, verified by reproducing it against pre-fix code first; multi-plate courses honestly classify `UNMEASURED` pending #17; and a five-agent adversarial panel caught three same-day defects in the fix's own prose and tests, plus a suite-wide fixture blind spot (every layer sat at world (0,0)) — all repaired. Suite 513 → 549.

**2026-08-23 (s23) — the harness knows what is in front of it, and the model's positive half is measured.** `render_course` verified nothing about the foreground: it opened with a blind coordinate tap while `assert_emulator_ready` checked the emulator, which is how s21 got a confident `inactive` off the Android launcher. `assert_app_in_foreground` now brackets the flow — before the first tap, and again immediately after the frame is captured — and **raises rather than repairing**, so the precondition stays visible. `reset_first` is opt-in and *establishes* main-menu state rather than checking it: splash and main menu are byte-identical to `dumpsys window`, proven by two committed captures and a test asserting their identity. The defect was reproduced against pre-guard `8518b79` before the fix was trusted (launcher in front, dark frame → `validity='inactive'`; same input guarded → raise). A three-lens adversarial panel found five defects in the fix's own prose, the worst being a byte-identity claim cited to fixtures that did not contain it. Three mutants that had survived 567 green now fail. **Then the odd-rotation interior run landed** — `MODEL_SURVIVES:plate+port_only`, 7/7, bracketed at both ends plus a local control. W and SE rendered **active** at (-3,2) rot 1 after being dark in all six exhaustive corner sweeps, refuting the frozen position-blind `table` on two cells at once and discharging the rival's purpose. `MEASURED_RUNS` is now nine campaigns across three starter positions and both parities away from the corner, and the class-sweeping test confirms the conjunction reproduces every row with no free parameters. Suite 549 → 580.

**2026-08-24 (s24) — the library can build the courses real people build, and the record admits it has not measured them.** The corpus answered #16's arrangement half for free: **599 of 640 parsed courses carry two or more plates**, so single-plate is the exception in the wild and `graph.py` was answering ~94% of real course shapes from a nine-campaign record that never contained one. Applying the measured footprint at every plate's `world_hex_position` yields **zero collisions across all 640**, so `world + local` is a coherent global frame; **228 of the 241 four-plate courses use one set of positions**, now `plates.STANDARD_SQUARE`, generated by `scripts/probe_plate_arrangement.py` rather than typed. New `traxgen/layout.py` builds a course from an explicit plate layout — the write path was the only gap, since serializer, parser and validator all already handled N layers — and a single-plate layout is pinned byte-identical to `generate_minimal()`. **The multi-plate courses this made reachable exposed a defect one level up from s21's.** `classify_pair` sends only *cross-layer* pairs to `UNMEASURED`, so a same-plate pair on a four-plate course was answered from the single-plate record, and `START_GOAL_CONNECTED` fired **ERROR** on the exact arm the add-a-plate probe exists to render — a false claim asserting one reading of #17. Reproduced against pre-guard code, then fixed at the `start_goal_status` level. Found by the panel, not the author. **A three-lens adversarial panel graded the work after it was green and self-reviewed** (#19's seventh firing) and returned six false sentences plus 28 surviving mutants; nine of the ten retested now fail, and the tenth is conceded in a test docstring rather than papered over — no offline test can distinguish a corrupted `STANDARD_SQUARE` from the real one, only re-running the probe can. Suite 580 → 619, committed at `30dfb4d`. **Zero renders spent.**

**2026-08-24 (s25) — the record says which board it was measured on, and a panel catches the fix reopening the defect it closed.** `MeasuredRun` gained `plate_offsets`: where every baseplate sat, relative to the starter's own plate, in the course that was rendered. All nine campaigns carry `((0,0),)` — the starter's plate and no other — and that is now part of `measured_run`'s lookup key rather than a precondition living in the builders. s24's baseplate *count* in `start_goal_status` is gone: a multi-plate course misses the record and comes back `UNMEASURED` because nothing measured it, not because a rule says so. The count also carried a latent hole — it counted `BASE_LAYER_PIECE` alone while `b8052e4` and `validator.py` count `BASE_LAYER` too, so the certified course plus one empty `BASE_LAYER` returned CONNECTED from a single-plate record (enacted, not argued). `plates.BASEPLATE_LAYER_KINDS` now has one home and both modules import it. **The key is keyed on offsets rather than absolute world positions, and that was not a free choice**: absolute keying makes the s22 moved-board fixture return `UNMEASURED` for both the honest reading and the world/local confusion it exists to catch, so #26's blind spot reopens with nothing failing. It also makes explicit an assumption the shipped model has always carried and never written down — that a course's absolute world offset does not affect connection, which no render has tested. **The three-lens panel found that the fix reintroduced the s24 defect one shape over** (#19's eighth firing, and the first time two lenses converged on the same finding independently): `classify_pair` answers `DISCONNECTED` for non-adjacent pairs *before* consulting the record, s24's count sat above that short-circuit and the lookup sits below it, so every non-adjacent pair on a multi-plate course was claimed measured-disconnected at ERROR. The suite stayed green because the one multi-plate fixture used an adjacent goal. Fixed by consulting the record before any verdict. The mutation lens found 22 survivors, most of them one coincidence: every offset the suite compared was the zero vector. **Claude's own mutation battery then reported 0 of 19 caught, including its control** — a bad pytest flag made every run collect nothing, and "no failures" parsed as "survived" (new candidate #32). Repaired: 17 of 19 caught, two conceded in writing. Eleven new tests. Suite 619 → 639. **Zero renders spent.**

Artifact status: nothing was lost with the deleted `~/Desktop/Hub` checkout — `scripts/trace_both.py` and both oracle fixtures (`4YCV8JHLX7.course`, `X3WEQ6F296.course`) are committed and present in the fresh clone (verified via project tree, 2026-07-22).

## Phase 1 definition of done

`python -m traxgen generate --set vertical-starter` produces a `.course` binary that: (1) is accepted by the share-code upload endpoint, (2) loads in the official app via that code without errors, (3) uses only PRO Vertical Starter-Set (26832) pieces, (4) has a valid ball path from a starter to a goal, (5) fits on the set's 4 baseplates. Phase 1 closure is measured against this.

**Explicit v1 non-goals:** interesting tracks, variety, physics simulation, aesthetics, race mode, perpetual mode. Phase 1 proves the pipeline works end-to-end — nothing more.

## Sequenced work in flight

1. **Design the port-taxonomy instrument** — the attack on open unknown #7, and next session's opening design work (design, not locked design — lock what survives into `decisions.md` then). The 93-member `TileKind` catalog killed pairwise sweeping in one command: 93² pairs × 36 cells ≈ 311k renders. The decoding unit is the **port** — a (side, height, direction-of-travel) opening — and kinds collapse onto a small archetype vocabulary (13 members are baked-in rotation/state variants of three pieces; curves and tunnels almost certainly share an interface; the mirror collapse is *not* assumed — symmetry models have been refuted by render twice). Three instruments, cheapest first: **(a) corpus mining** — the format speaks ports natively for rails: every rail serializes two `RailConstructionExitIdentifier`s carrying `side_hex_rot` and `exit_local_pos_y` (verified in murmelbahn source, 2026-08-10), so parsed shared courses yield (tile, side, height) attachment points off disk — free, positive-evidence-only. **(b) Vision reads** — the app renders all 93 kinds regardless of ownership; contact-sheet courses screenshotted through the harness and read by Claude yield per-piece port hypotheses, committed as auditable artifacts (screenshot beside structured read), allowed to prioritize the render queue, never to write `graph.py`'s table — vision-predicted cells stay `UNMEASURED` until rendered. **(c) Chain probes** — starter→X→goal_rail renders confirm; a dark button blames the chain, not a joint (conjunction), so hold one joint known-good and vary the other. **Pre-registered calibration first:** compose vision reads of STARTER and GOAL_RAIL; test whether they predict the measured six-row table. Suspicion declared in advance: geometry under-predicts parity — either outcome moves us (observations #20, #21). **Gate:** the zoom probe — at the default camera a two-tile course is a sliver of the frame (observations #18); port reading needs pixels; the first experiment is whether scripted zoom or the editor view yields legible close-ups. *learning*. **2026-08-18 (s20): instrument (a) is built, run at full corpus scale, and calibrated** — 43,375 observations / 74 kinds / 652 courses, zero renders; see the session-log row. Its calibration passed (both oracle fixtures parse to the certified geometry; `classify_pair` returns CONNECTED). Port-record storage shape and corpus policy are locked in `decisions.md`. What remains of item 1: the vision-channel gate (zoom probe), chain-probe oracle semantics (well-formed vs completes-the-run), and where the committed record store lives in the repo.
2. **M5.a proper: traversal over the track graph** — `graph.py` currently classifies *pairs*; a real track graph needs edges between arbitrary placed tiles and a walk from starter to goal. Blocked in practice by open unknown #7 (per-tile-type connection rules): the STARTER→GOAL_RAIL mechanism is measured, everything else is not — item 1 is the attack. *dependency* — the next generation step (`generate --set vertical-starter` producing something bigger than two tiles) needs it.
3. **Wire polling into `render_course`** — `wait_until` / `wait_for_node` exist and are tested offline; the four native steps still use fixed sleeps. *blast radius* — smallest change that retires the 2026-08-07 stopgap. **Urgency restored 2026-08-10**: a locked screen throttled the occluded emulator mid-queue and a fixed sleep fired into a loading screen (the frame guard caught it, one auto-resume closed it). Polling would have absorbed the throttle instead of tripping on it, which is exactly the unattended-run failure mode the queue runner otherwise removes. **2026-08-23 (s23) scope correction:** this item does *not* close the splash-screen failure. `wait_for_node` reads `uiautomator`, and the Unity game surface is opaque to it — the same blindness the foreground guard hit, one layer on. `wait_until` takes an arbitrary `dump_fn`, so the primitive fits; the predicate has to be pixel-stability, per the triggered review below. The s23 run hit exactly this: the opening certified control was refused at `white_frac 0.660` (a loading screen), costing a whole campaign and one re-run.
4. **Run the #17 2×2** — the design is locked in `decisions.md` and the library can build it. Two things gate it now, not one. Item 5 below is the render-path gate. The new one, found by the s25 panel: **arm 1's result is not expressible in the record.** Arm 1 puts the goal on a *neighbouring* plate, so `classify_pair` sends it to `UNMEASURED` at the cross-layer check before the plate key is ever built — and `MeasuredRun` has no field for which plate the goal stood on, so a render that comes back active has nowhere to be written down. s25 made arm 2 recordable and arm 1 not. Deliberately **not** designed blind: what the goal-side term should be depends on what the render says about how membership composes across a boundary, which is the question. The honest sequencing is either (a) render the 2×2, hold arm 1's result in the session, and design the field around the measured answer, or (b) design a minimal goal-plate term first and accept that it may be the wrong shape. Colby's call; (a) is the project's usual order (corpus/render proposes, code disposes). *dependency* — decides how the generator places tiles on multi-plate courses; feeds #16 and item 6.
5. **A pixel-stability predicate for the two Unity-surface waits** — promoted out of the polling item by s23's finding, and now the standing gate on item 4's render phase. The s23 campaign died when the opening certified control was refused at `white_frac 0.660` — a loading screen — and `probe_plate_membership.py` still has no resume path, so a refused render voids a whole campaign. `wait_until` already accepts an arbitrary `dump_fn`; what is missing is a predicate that samples successive frames and waits for them to stop changing. *blast radius* — the last unguarded moment in the render path and the only one that still costs runs.
6. **A base layer that models the baseplates instead of assuming them** — everything *rendered* so far sits on one plate at world (0,0); the standard four-plate arrangement is now measured from the corpus but has never been rendered. Real courses add plates and connect them only parallel-side to parallel-side, never perpendicular. **2026-08-24: the arrangement half is answered** — `plates.STANDARD_SQUARE` plus the zero-collision tiling result (see Current state and answered #16), and `traxgen/layout.py` takes the arrangement as input rather than a constant. What remains is the *semantics*: how connection composes across a plate boundary, which is item 4's question. *dependency* — open unknown #16; feeds #3 and the dropped `BASEPLATE_COVERAGE` rule. Raised by Colby 2026-08-08.
7. **The two remaining s24 panel recommendations, each enacted green and each a real change** — (a) `build_variant` delegating to `build_course`: −39/+12 lines, and it makes the sweep's byte-identity guards real (a generator-drift mutation goes from 1 failure to 4; `_assert_control_matches_generator`'s docstring currently admits it cannot catch that drift, and delegation is what makes it false in the good direction). (b) Moving `STANDARD_SQUARE` out of `plates.py`, whose stated concern is which local cells each `LayerKind` occupies, not how several plates sit together — note s25 moved a constant *into* that module for the opposite reason (`BASEPLATE_LAYER_KINDS` is a per-`LayerKind` fact; `STANDARD_SQUARE` is not), so the two moves are one consistent rule rather than two tastes. *relatedness* — one epistemics-and-cohesion batch. The third recommendation, `MeasuredRun` recording the plate set, **landed 2026-08-24 (s25)**.
8. **Teach the library the SkyTrax era** — supersedes the old "accept v7, skip the u32=13" framing (that u32 turned out to be a relocated *field*, not a constant — see answered #13). Per murmelbahn upstream `8d7974a`: `CourseSaveDataVersion` 6–7, `CourseElementGeneration` ids 10–13, `TileKind` ids 94–126, and the skytrax course body (generation after metadata; layer `(id, kind, position, int small-stacker height)`; connector array; sha256 tail) — `types.py` + parser, source commit noted per `.cursorrules`. Rides with it: commit `scripts/mine_ports.py` (+ offline tests) and the harvest/probe scripts, per the locked corpus policy. *relatedness* — one schema-fidelity batch, now real upstream-documented work rather than a byte-skip.
9. **Re-verify `local_hex_position` honoring under the harness** — promote the tentative finding to resolved or falsify it; the manual-loop evidence is tainted by the clipboard failure. *bake-time* — cheap now that sweeps are routine.
10. **Dedicated lint-cleanup session** — the 72 pre-existing ruff findings (re-counted 2026-08-24 by stashing s24's files and re-linting; the older "38" figure was scoped differently), plus whatever the three new scripts added, plus 2026-08-10's four new files. *relatedness* — batch as one session; not mixed into feature work. Note the standing practice that keeps this bounded: each session lints only its own new files (2026-08-10 verified its four were clean and confirmed the rest pre-existed by stashing), so the debt is inherited, never grown.

## Triggered reviews

- When a milestone completes → refresh the README's status paragraph (it drifts otherwise).
- When murmelbahn's schema changes upstream → update `types.py` to match and note the source commit in the docstring (per `.cursorrules`). **Fired 2026-08-18** — upstream `8d7974a` (PR #28) adds the SkyTrax era: v7 body, generation ids through 13, TileKind through 126. Scoped into Sequenced work item 5; retire this note when it lands.
- When `ROTATION_OUT_OF_RANGE` appears to false-positive on an app-accepted course → revisit the modulo-normalization hypothesis (open unknown #11).
- ~~When rendering in the emulator gets laggy → upgrade the harness from fixed sleeps to polling-based waits.~~ **Fired 2026-08-07**; stopgap applied and the polling primitives built. **Fired again 2026-08-10** via a different route — GPU throttling of an occluded emulator window, not general lag. Retire this row once Sequenced work item 3 (polling) lands.
- When the two Unity-surface waits (`after_load`, `after_render_load`) start failing → they cannot be polled via `uiautomator` (the game surface is opaque to it); a pixel-stability predicate is the only route. Not yet triggered — the IME step is what failed.

## Open questions

Still-open v1-scope unknowns, numbering preserved from the original PLAN.md for traceability (resolved/dissolved items live in the archived PLAN and in `decisions.md` where they hardened):

- **#1 Switch TileKind encoding** — state via TileKind vs separate field; verify by parsing an app-exported single-switch course.
- **#2 Rail `side_hex_rot` semantics** — de-prioritized 2026-06-12 (not the minimal-course blocker); matters again when the generator places explicit rails. **2026-08-10:** corpus mining (Sequenced work item 1) will read `side_hex_rot` at scale for free — distributional evidence arrives as a by-product. **2026-08-18: it did** — 43,375 observations; `side_hex_rot` is a world-frame cell edge, and subtracting the tile's `hex_rotation` recovers piece-intrinsic structure (26 kinds collapse to a single parity class of relative edges). The tile-relative reading is the meaningful one.
- **#3 Baseplate physical shape** — **answered 2026-08-21, from the corpus, zero renders.** A `BASE_LAYER_PIECE` occupies exactly **30 local cells** — `y ∈ [-6,0]`, `x ∈ [0,5]`, rows of 2/4/6/6/6/4/2 — across 640 parsed courses and 28,494 cell placements, and **no course ever uses a position outside that set**. `LARGE_LAYER` is 19 cells, `SMALL_LAYER` 7. Local (0,0) sits on two boundaries: its W, SW and SE neighbours appear **zero** times, while `LARGE_LAYER`/`SMALL_LAYER` show all six of theirs occupied (so the probe is not simply blind). Positive evidence only — "never observed in 28,494 placements" is not "the app forbids it" — but the render channel then confirmed the operative half: an off-plate goal cell does not connect (see #15). The constant lives in `traxgen/plates.py` as `MEASURED_FOOTPRINTS` (moved from the probe script in s22, with LARGE_LAYER and SMALL_LAYER measured alongside); regenerate with `scripts/probe_plate_footprint.py`. `BASEPLATE_COVERAGE` is now derivable, so its v1 drop can be revisited. Left open underneath: whether "off-plate" is physical or merely outside a canonical coordinate window — the new #17.
- **#4 Balcony world-coord resolution** — probe-first follow-up; until then balcony-touching rails skip span validation.
- **#5 Retainer ID assignment scheme** — ranges known, scheme unknown.
- **#6 GUID generation** — GUID=0 accepted by upload; app-side validation unverified beyond current evidence.
- **#7 Connection rules per tile type** — feeds the track graph; derive from physical specs + fixtures. **2026-08-10 sharpened:** 93 `TileKind` members make naive pairwise sweeping dead on arrival (~311k renders at 36 cells/pair); the decoding unit is the **port** — a (side, height, direction-of-travel) opening — and the attack is Sequenced work item 1: corpus-mined rail ports, vision reads of app renders, chain-probe confirmation, calibrated first against the measured STARTER→GOAL_RAIL table. **2026-08-18: the corpus instrument ran at scale** — 74 kinds observed; 26 show clean single-parity-class intrinsic port sets (19 even, 7 odd, none mixed); exit heights are perfectly quantized (`y = 0.27 + 0.36k`, 8 levels, zero exceptions in 43,375 records); the spread-class kinds are bimodal, cause open. Catalog note: upstream `TileKind` now runs to id 126 (SkyTrax pieces 94–126), so the 93 figure was already stale — the port reframe gets more necessary, not less.
- **#8 `THREE_ENTRANCE_FUNNEL` mapping** — best-guess; confirm from a fixture using the piece. **2026-08-18: corpus evidence** — 34 observations across real courses put its entrances at relative edges {0,2,4}, balanced. Three entrances at alternating edges; render has never tested it, but the mapping is no longer a guess.
- **#9 Track-graph representation** — design when #7 is answered enough to be worth it.
- **#10 `LayerKind` semantics** — `BASE_LAYER` vs `BASE_LAYER_PIECE` distinction; what `LARGE_GHOST_LAYER` is.
- **#11 Rotation modulo hypothesis** — see triggered review above.
- **#12 Which upload headers are strictly required** — full set works; stripping untested.
- **#13 Schema v7 delta** — **answered 2026-08-18.** v7 is the SkyTrax format, and murmelbahn upstream now documents it (`8d7974a`, `lib/src/app/skytrax.rs`). The mystery `u32 = 13` is `CourseElementGeneration::SkyTrax` — the generation *field*, relocated from its v4 position to just after metadata; "13" was its value in the fixtures, not a constant. The rest of the delta: layers store `(id, kind, position, integer small-stacker height)`; tiles carry both power and light-stone fields; a vertex-connector array and a 32-byte sha256 tail close the file (decomposition byte-verified on both oracle fixtures). Library support is queue item 5 (redefined).
- **#14 Why four of six adjacent directions have no connecting rotation** — **answered 2026-08-08.** The suspect was right: the starter's `hex_rotation` was pinned at 0 for all 42 cells of the 2026-08-07 sweep, and it moves which directions connect. The four dead directions were dead *at that starter rotation*, not in general — NE, dead at all six rotations on 2026-08-07, is active at s=1. The apparent partiality was never in the rotation rule; `g = (d + 1) % 6` is a goal-side rule that has never once been violated. What is restricted is the live direction set, which is a starter-side property. Superseded by #15, which asks what that set actually is.
- **#15 What determines the live direction set, and why does its size change?** — **ANSWERED 2026-08-21.** `live(s, pos) = plate_available(pos) ∩ {(r + s) % 6 for r in STARTER_PORTS}`, with `STARTER_PORTS = {0,2,4}` (corpus, n=380) and `plate_available` read off the 30-cell footprint (corpus, n=28,494). Both terms were measured before any render. At the corner that gives `{E, NW}` at even rotations and `{NE}` at odd — the measured table exactly, **sizes included**. The 2-vs-1 asymmetry was the plate corner, not the starter; the set does not rotate because the plate term is world-fixed while only parity moves; and every symmetric model was refuted because a corner is not symmetric. Tested by prediction, not fit: the **edge** run at (0,1) refuted the port-only model (off-plate but port-allowed cells stayed dark) and the **interior** run at (-3,2) refuted `graph.py`'s own position-blind table (**SW rendered active** after six exhaustive sweeps of darkness). 14 cells, two starter positions never previously rendered, every verdict declared in code first, zero misses, harness bracketed at both ends of both runs. Locked in `decisions.md`. Residue, **narrowed 2026-08-23 (s23)**: the odd-parity interior prediction `{NE, W, SE}` was the open half and is now measured — all three rendered active at (-3,2) rot 1, 7/7 with the run bracketed at both ends, and the position-blind `table` died on W and SE together. What remains unclaimed is narrower: only two starter positions off the corner have ever been rendered, goal rotations were not swept at either (each direction was rendered only at its connecting rotation), and GOAL_RAIL's own intrinsic port is *derived* as rel = 2 — which is why `g = (d+1) % 6` carries no starter term — and never measured, because the corpus structurally cannot see it (0 records in 43,375: GOAL_RAIL carries its own rail, so it never appears in a `RailConstructionExitIdentifier`) — the counterexample that scoped the corpus lock in `decisions.md` to a visibility test rather than an ordering rule. Superseded framing follows. **The pattern was answered 2026-08-10; the mechanism was not.** All six starter rotations are now exhaustively swept (36 cells each, controls active at both ends of every run), and the live-direction set is a clean **parity** of the starter rotation: even → {E rot 1, NW rot 3}, odd → {NE rot 2}. s=4 and s=5 were *predicted before rendering* and both landed, so this is a surviving forward prediction rather than a fit (observations #20). What remains open is why: why parity at all, and why the even set has two members and the odd set one. Every symmetric model proposed has been refuted by render, and that asymmetry is precisely what refutes them. Practically closed for generation — `traxgen/graph.py` carries the full table — and the residue is physical understanding, likely bearing on #7 (per-tile connection rules) rather than on anything v1 blocks. **2026-08-18, a mechanism seed from independent data:** corpus mining shows the STARTER's ports live on even tile-relative edges only ({0,2,4}, balanced) — and an even-only intrinsic port set flips its world-edge parity with rotation parity, which is exactly the *shape* of the measured table. Declared as hypothesis under the #20 discipline: it earns nothing until it predicts goal-side geometry in advance of renders (protocol is next-session work), and it does not yet explain set sizes 2-vs-1 — the GOAL_RAIL's own port class is the thin term (goal rails rarely take explicit rails, so the corpus barely sees it).
- **#16 Baseplate arrangement is assumed, not modelled** — every course measured to date sits on the standard PRO Vertical base: four plates in a square. That is a constant in the code and an assumption in the evidence. Real courses add plates and arrange them differently, and plates connect only parallel-side to parallel-side, never perpendicular — so arrangement is constrained, not free. Needs either a reverse-engineered base model or a representation that takes the arrangement as input. Overlaps #3 (physical shape) and #10 (`LayerKind` semantics); distinct from both in being about *adjacency between plates*. Raised by Colby 2026-08-08 while reading the sweep results. **2026-08-21: promoted from "needed eventually" to "blocking correctness."** Plate membership is now a *measured* gate on connection, so a connection claim is a function of the course's declared plate set — and plates are addable (Colby, 2026-08-21; `GDZJZA3J3T` carries 15 `BASE_LAYER_PIECE` layers, each with its own `world_hex_position`, which is what adding plates looks like on the wire). `graph.py` keyed on starter rotation alone and was wrong off the corner until s22; single-plate courses now classify against the measured record, and **multi-plate courses classify `UNMEASURED`** — the honest floor until this unknown and #17 decide how membership composes across plates. The corpus can also answer this one cheaply: 640 courses carry real multi-plate arrangements and their relative world positions are readable without a render. **2026-08-24 (s24): the arrangement half is answered, zero renders.** 599 of 640 parsed courses carry two or more plates; **zero of the 640** show two plates claiming one world cell when the 30-cell footprint is applied at each `world_hex_position`, so `world + local` is a coherent global frame rather than a per-plate convenience; 120 distinct pairwise plate deltas occur, and **228 of the 241 four-plate courses use exactly `{(0,0), (3,-6), (5,0), (8,-6)}`** — measured as a *set*, not inferred from four marginal frequencies. Of the 11 distinct four-plate sets, **8 are that shape translated and 3 are not**: two are a straight line of four (normalised `((0,0),(5,0),(10,0),(15,0))`) and one is an L. The square is the mode, not the only shape. That constant is `plates.STANDARD_SQUARE`, regenerated by `scripts/probe_plate_arrangement.py`. Of the three directions dead at the plate corner, exactly one observed delta covers each: `(3,-6)` puts plate under **W and SW**, `(5,0)` under **SE**, and the standard square contains both. `traxgen/layout.py` now takes the arrangement as input. What stays open is the *semantics* — how connection composes across a plate boundary — which is #17's question, and until it is answered `start_goal_status` holds every multi-plate course at `UNMEASURED` (`decisions.md`, s24). Note also what is **not** answered: "plates connect only parallel-side to parallel-side" is still Colby's physical observation, not something read off the corpus.

- **#17 Is "off-plate" physical, or outside a canonical coordinate window?** — new 2026-08-21. Renders confirm that a goal cell absent from the corpus footprint does not connect, but not *why*. Either the cell has no plate under it, or the format stores layer-local coordinates inside a canonical window (with `world_hex_position` absorbing the offset) and our variants were simply malformed. The operational consequence is identical today, which is why it did not block #15 — but it decides how the generator places tiles on multi-plate courses. Colby's own test separates them: add a second plate beneath a currently-dead cell and re-render. If it lights, "off-plate" is physical. Feeds #16 and sequenced item 6. **2026-08-24 (s24): that framing is superseded, and the replacement is locked in `decisions.md`.** Adding a plate *and* addressing the cell in the new plate's local frame changes both factors at once — plate presence and coordinate validity — so both readings predict it lights and a positive result discriminates nothing (#21 in the mirror). The experiment is a **2×2** over {plate present, plate absent} × {in-window address, out-of-window address}: s21 already measured plate-absent + out-of-window (dark), plate-absent + in-window is impossible by construction, and the two new arms render the *same world cell* both ways on one four-plate course. Arm 1 vs arm 2 isolates addressing; arm 2 vs s21 isolates plate presence. `traxgen/layout.py` deliberately does not validate local positions against the footprint, because arm 2 is exactly the course a footprint check would refuse. Five renders including the controls; the design is done and unspent. **2026-08-24 (s25): the design is intact and one arm is not yet recordable.** `MeasuredRun` now carries the course's plate layout, so arm 2 — goal addressed out-of-window on the *home* plate — is a configuration the record can hold. Arm 1 is not: its goal sits on a neighbouring plate, `classify_pair` sends cross-layer pairs to `UNMEASURED` before the plate key is built, and no field records which plate the goal stood on. So a render that comes back active on arm 1 answers the question for a human and leaves nothing for the code. Not designed blind on purpose — what the goal-side term should be is downstream of what the render says about composing membership across a boundary. See sequenced item 4.

**Deferred cleanup** (small, non-blocking — from PLAN.md, trimmed to what's still live): `CourseElementGeneration` ids 10–13 missing from `types.py` (`AUTUMN_2024` through `SKYTRAX`, per upstream `8d7974a` — bit 34 corpus courses on 2026-08-18, shimmed in the miner; real fix rides with Sequenced item 5); pillar id u32-vs-i32 read; mounted-only balcony counting assumption; walls with unexpected hex distance silently skipped; starter/goal override API (design when a consumer forces it); render outcomes should be recorded as text, screenshots deleted; `android.py`'s `DEFAULT_SCREENSHOT_DIR` hardcodes the deleted `~/Desktop/Hub` checkout path and silently recreates it via `mkdir` — make it repo-relative or config-driven (`scripts/sweep_goal_rotation.py` routes around it with a repo-relative default, but the library default is unchanged); `knowledge/environment.md`'s emulator boot line uses `$ANDROID_HOME`, which is not exported in a default shell — fixed there 2026-08-07, but `resolve_context()`'s fallback is what actually saves the library; a second AVD `traxgen_test` exists and is undocumented — describe it or delete it; `knowledge/environment.md` documents the `${ANDROID_HOME:-...}` fallback for the emulator binary but not for `adb`, which invited exactly the half-application that hung a session's boot-wait on 2026-08-21 (observations #25); `sweep_goal_rotation.py`'s `FAR_CONTROL_POS = (0,2)` is off-plate, so the distance-2 controls measured cell invalidity rather than distance — pick an on-plate distance-2 cell before that control is trusted again (`graph.py`'s docstring citation of those controls was withdrawn in s22 — `classify_pair`'s non-adjacent claim now rests on the mechanism alone); `probe_plate_membership.py` has no resume path, so a hole costs the whole campaign — s23 added upload retries in the library, which removes the common cause but not the gap itself (a refused *render* still voids the run); `scripts/probe_baseplate_arrangement.py` is superseded by `probe_plate_arrangement.py` and is **actively misleading**, not merely stale: it filters `LayerKind.BASE_LAYER`, which matches **zero** layers in its own named fixture (GDZJZA3J3T's 15 baseplates are all `BASE_LAYER_PIECE`, per the `b8052e4` lock), so it reports "Found 0 BASE_LAYER layer(s)" and then prints "No overlaps across 0 world-hex positions" — a green over an empty set, in the script a future session would reach for. Retiring it is a human's call, so it is left in place, named in the new probe's docstring, and pinned by a test that *runs* it (repairing the filter fails that test, which is the intended trigger); `layout.CERTIFIED_LAYER_HEIGHT`'s corpus-vs-certified tension is documented in `decisions.md` and the parameter exists, but no run has yet used -0.2; `traxgen/graph.py`'s `MEASURED_LIVE_DIRECTIONS` carries a `plate_offsets` filter clause that is provably dead today (every row shares one layout) and becomes load-bearing the moment a multi-plate campaign is recorded at the corner — flagged in a comment rather than left to look like coverage, retire the comment when the #17 run makes it live; re-photograph the starter-set manual pages, rail-height table first (primary evidence for Δheight = 4; photos were never committed; verified no .gitignore rule involved — PRO 26832 pages worth adding too).

## Session log

Backfilled from the dated history embedded in PLAN.md — earlier sessions left no per-session records. Date-based naming (this project's history is already date-keyed).

- **2026-04-22:** Sideload ruled out architecturally — iOS app accepts no local data-in path. M6 reframed around the share-code API. → reverse-engineer upload.
- **2026-04-24:** Upload API captured via mitmproxy under a pre-declared 4-hour budget (endpoint, headers, dedup-by-content-hash). M6.a shipped: `uploader.py`, exception hierarchy, mock-server tests, live canary. First uploads rendered tiles but never rails — new unknowns surfaced; manual loop identified as the bottleneck. → automation (M6.c).
- **2026-04-25:** M6.c shipped — AVD `traxgen_m6c`, Play-Store GraviTrax, `android.py` harness, play-button validity oracle calibrated on X3WEQ6F296/MT756NLLMI. → resume M6.b under automation.
- **2026-06-12:** Rail-model breakthrough — valid courses have `rail_count = 0`; GOAL_RAIL carries its own rail; adjacency + goal rotation is the connection. `FLW4TMLP5V` is the first app-certified generated course. v7 delta narrowed to one u32. Clipboard failure mode discovered — pre-discovery manual observations demoted to tentative. → M5.b-fix.
- **2026-07-22 (migration):** Project migrated onto Allostatik. `docs/PLAN.md` folded into the canonical files and archived; `.cursorrules`, README, `docs/refs/` classified live-in-place; CLAUDE.md rewritten as adapter over the allostatik files; `docs/refs/android-automation.md` written from the archive + `android.py` (closing the deferred item); original checkout at `~/Desktop/Hub` confirmed deleted — artifact survival flagged for verification. → M5.b-fix.
- **2026-07-27 (repair):** Canonical-vs-repo drift found and cleared. `plan.md` still listed M5.b-fix as next when `generate_minimal()` had emitted the validated no-rail shape since `f6e5717` (app-certified `FLW4TMLP5V`) — reconciled at `19abcf4`, which also filled a placeholder in `knowledge/docs/README.md`. `decisions.md` locked Python 3.12 as "pinned `.python-version`", but scaffold boilerplate (`a757183`) had gitignored that file into never existing and fresh clones silently resolved to CPython 3.14.4 — fixed at `9b028e4`, with `requires-python` left at `>=3.12` as the consumer compatibility floor. Suite 279 passing / 1 deselected on 3.12.13, matching the pre-move baseline. Close routine did not run; row backfilled 2026-08-04. → goal-rotation sweep.
- **2026-08-03/04 (rename):** `allostat/` → `allostatik/` for the upstream Allostatik 0.3.0 rename — one Claude Code session spanning both dates. `7158605`: migrate script applied — folder `git mv`'d wholesale, 25/48 fences flipped, 7 `@`-imports repointed, 39 in-folder replacements; script and independent verify both green. Zone 4 (host-project references outside `allostatik/`, which the script reports but by design never edits) cleared by hand across two commits: `f8ab4bf` repointed `scripts/update_docs_20260727.py`, and `4988c41` repointed 3 CLAUDE.md preamble mentions that turned out to be live path pointers rather than prose — the orient step had been directed at `allostat/workflow.md` and `allostat/plan.md`, neither of which existed post-rename. `docs/PLAN.md`'s archive banner and the `docs/refs` mentions were left deliberately as historical record. Close routine did not run; row backfilled 2026-08-04. → goal-rotation sweep.
- **2026-08-04 (backfill):** Closed the session-log gap left by the two preceding sessions. `plan.md` gained rows for 2026-07-27 and 2026-08-03/04; `decisions.md`'s rename row marked `[AMENDED]`; `observations.md` promoted #12 (a check can pass while asserting something false — three firings) and added candidates #13 and #14. Commits `0b9e989`, `7535813`. Key finding: the `Claude-Session` trailer proved `7158605`/`f8ab4bf`/`4988c41` were one session spanning two dates, putting missed closes at two rather than three — below the double-loop threshold, so the freshness-check repair was deliberately *not* attempted. That repair and #13 are upstream Allostatik work. Sweep untouched. → goal-rotation sweep.
- **2026-08-07 (goal-rotation sweep):** Sweep built, run, and partially answered. `scripts/sweep_goal_rotation.py` + 27 offline tests; 36 adjacency cells plus a positive control and six distance-2 negative controls, with all five falsification conditions pre-declared in `classify()` and tested before the first render. **Result: `rot = (d + 1) % 6` is the sole surviving affine rule** — E rot 1 was the discriminator that separated the two chiralities the single 2026-06-12 geometry could not. Four directions have no connecting rotation, which the rule mispredicts; recorded as open unknown #14. Run one aborted after a single render when the control read `inactive`: a cold-booted AVD had not laid out the fullscreen IME inside the 0.3 s `after_text`, so `ime_ok` tapped an unready view and the oracle sampled a keyboard. Tap coordinate exonerated in one command via `uiautomator` bounds (`[2216,252][2384,378]` contains (2270,305)); the fixed-sleep triggered review fired. Stopgap (`after_text` 1.5, `after_tap` 0.8) plus a closing-bracket control; polling primitives (`wait_until`, `find_node`, `wait_for_node`) built and tested offline against a captured dump but not yet wired in. Separately: the byte-identical control returning a *new* share code exposed that `generate_minimal()`'s certification had never been compared to the certified artifact — diffing `FLW4TMLP5V` showed the only divergence is the title string, so the geometry stands and content-hash dedup is confirmed. The resume run then produced a **false `active`** from a splash screen, which without the closing bracket would have been reported as `MODEL_WRONG`; `white_frac >= 0.50` frame guard added to the oracle, calibrated on measured screencaps. Commits: see `Claude-Session` trailer. → starter-rotation sweep.

- **2026-08-08 (starter rotation: three sweeps, one refuted model):** Opened with the drift-check passing as a *real diff* for the first time — both claude.ai fields written to `/tmp` and `diff`'d against `allostatik/deployed/`, `IDENTICAL` each. Session ran with direct repo file access (device bridge) but no `adb`/emulator on that side, so every render came from Colby's shell one command at a time. **Sweep 1 — starter rotation, 9 renders.** The handoff's design (hold the mispredicted NE rot 2, sweep the starter) was replaced before coding: a null result there cannot separate "direction unreachable" from "coupling real but never yields rot 2 here". Holding a *known-active* cell (E rot 1) makes both outcomes informative. Result: E rot 1 active at s ∈ {0,2,4}, dead at {1,3,5}; NE rot 2 — dead at every rotation on 2026-08-07 — active at s=1; NW rot 0 backfill inactive, completing the 42-cell grid with one-rotation-per-direction intact. **The run's own printed verdict asserted a mechanism its data refuted** (`{s, s+2} mod 6` excludes E at s=2, where E connected): pre-declared trigger sound, attached free-text explanation not. **Sweep 2 — parity model, 5 renders.** A model fitted to all prior data (exit pair `{E,NW}` even / `{NE,W}` odd, `g` unchanged) was tested by declaring each cell's verdict *in code* before rendering. Three of four held: the app-certified geometry does break at s=1, the rival `g = (d + 1 + 3s) % 6` is dead, and s=2 restores s=0 exactly. The fourth missed — W rot 4 dead at s=1. **Sweep 3 — full 36-cell at s=1, 43 renders, 18.2 min.** Exactly **one** connecting cell at s=1: NE rot 2. Both chiralities of the exit-pair model are refuted (W and SE dead at all rotations), so the pair framing is gone; what survives is that `g = (d + 1) % 6` has never once been violated across two exhaustively swept starter rotations. M6.b closed on that basis; #14 answered, #15 and #16 opened. **Refactor:** rather than a third near-duplicate script, `sweep_goal_rotation.py` gained `--starter-rot` (control pinned at s=0 and excluded from the per-direction tally, because the certified geometry is inactive at s=1), and `_goal_variant` was retired for the shared `build_variant`. Suite 332 → 397. HTTP 520 struck once more mid-batch (observation #15, second firing); the resume path closed the hole in one render. → M5.c, and the s=2..5 characterization.

- **2026-08-10 (the queue runner, M5.c, and parity):** Ran on the Cowork surface with the desktop Filesystem MCP — direct repo read/write, but **no shell on the Mac side**, so every test run, render and git command came from Colby's terminal. Open checks passed four-for-four; the deployed-surface drift check ran as a real diff for the second consecutive session (`IDENTICAL` on both claude.ai fields). **The session's frame was Colby's, not the handoff's:** he asked whether the sweeps could run unattended — "have a plan and let it go" — and that reframed sequenced item 2 from *a run to perform* into *a layer to build*. **`scripts/run_sweep_queue.py`** (+22 offline tests, injected test doubles for `run_sweep`/`reset`/`sleep`) chains sweeps: reset + 35s settle, one auto-resume for a mechanical hole, hard stop on anything questioning the harness. `sweep_goal_rotation.py` gained `default_output_dir()` (shared with the queue rather than duplicated) and now **persists its verdict into the sidecar** — previously a harness abort and a 520 hole both read as `INCOMPLETE`, indistinguishable to any machine reader, which is the distinction the queue's gates turn on. **One launch, ~80 minutes unattended, all four remaining rotations characterized**, with two self-repairs firing in production: a 520 at s=2 (observation #15, third firing) and a throttled render at s=4 caused by the lock screen occluding the emulator — the frame guard refused the near-white frame rather than misreading it (#17, second firing). **#15's pattern is answered: parity.** Even starter rotations give {E rot 1, NW rot 3}, odd give {NE rot 2}; s=4 and s=5 were declared in conversation *before* their renders and both held, making this a surviving forward prediction rather than a fit. The mechanism — why parity, why sizes 2 and 1 — stays open. **M5.c shipped in parallel**: `traxgen/graph.py` separates the goal-side rule (a function) from the live-direction table (data, refusing to interpolate), classifies three-valued (`CONNECTED`/`DISCONNECTED`/`UNMEASURED`), and adds `PlacedTile` + world-coord iteration; `HexVector.direction_to()` inverts `neighbor()`. Validator rule **`START_GOAL_CONNECTED`** registered with severities mirroring the epistemics — ERROR only where every pair is *measured* disconnected, WARNING for unmeasured space, silent on railed courses (unmodeled, #2/#7). It had an unplanned live outing: the s=2 sweep ran with the rule active and correctly pre-classified all six distance-2 controls, which then all rendered inactive. Suite 397 → 478. Also discussed and captured: containerization (rejected for the Mac, trigger named), the inventory-as-build-palette user story, and special-case handling as dimensions-with-defaults rather than a special-case class. **Post-close addendum:** Colby's permutation question — "going through each permutation to discover validity is going to be impossible" — reframed #7 before the handoff was pasted. One command put the catalog at **93 `TileKind` members**, killing pairwise sweeping (~311k renders); the surviving unit is the **port**. His proposal: the harness is also a camera — the app renders all 93 kinds regardless of ownership, and Claude reads the screenshots, at scale, as hypothesis-compiler only. A sandbox check of murmelbahn upstream verified the format already speaks ports for rails (`side_hex_rot` + `exit_local_pos_y` on every `RailConstructionExitIdentifier`), unlocking corpus mining as the free first instrument. All three sequenced as new item 1 with a pre-registered calibration against the measured STARTER→GOAL_RAIL table. Amendment landed via exact-match patch script (observation #8's mode) after the desktop bridge dropped mid-close and reconnected. → port-taxonomy instrument design (item 1), then M5.a traversal.

- **2026-08-11/18 (s20 — corpus miner; ports are parity-classed):** One session, two sittings, on the Cowork surface. Open ran five-for-five, with a first: the **L1 drift check closed as a real comparison** — the MCP allowlist's claude-config root proved to be an empty husk (canonical moved to `~/Claude/Projects/claude-config`; repoint still pending), so the file came in via a device-VM folder grant and matched the deployed prefs line for line, newest-edit marker on both sides. Both claude.ai fields byte-identical (third consecutive session). Session-naming convention declared by Colby and captured to `workflow.md` Working notes (`936dc2d`). **Item 1's instrument (a) went from design to run-at-scale in one arc.** `scripts/mine_ports.py` (sandbox prototype; delivered in-conversation; commit owed with offline tests) parses rails and joins each exit to its tile tower through layer *and* tile-carried retainers; calibration passed on first contact — both v7 oracle fixtures parse to the certified geometry and `classify_pair` returns CONNECTED, corpus channel agreeing with the render channel on every artifact both can see. The v7 parse wall turned into the session's biggest find: **murmelbahn upstream `8d7974a` documents v7 = SkyTrax** — #13 answered, catalog to 126 kinds, item 5 redefined, and the schema triggered-review fired. **Corpus at scale:** `POST /api/buildable` with a maximal inventory enumerated murmelbahn's cached corpus (820 codes, 2023-01 → 2026-08-10, both v7 oracles present — the deployment parses SkyTrax); 808 fetched to `~/Claude/Projects/traxgen-corpus` (12 whitespace-padded duplicate keys skipped, every clean twin fetched), all unique by content; versions v2×77 / v3×332 / v4×191 / v5×177 / v7×31. Mined 652 (+30 rail-less; 49 SkyTrax-kind skips, fail-loud by design; 77 v2 refused by declared version — a separate schema family the v4-path misparses into garbage). **Yield: 43,375 port observations from 36,668 exits across 74 kinds — zero renders spent all session.** Findings: exit heights perfectly quantized (`y = 0.27 + 0.36k`, k ∈ {0,1,2,4,7,8,14,21}, no exceptions); **26 kinds collapse to one parity class of tile-relative edges (19 even, 7 odd, none mixed)** — tile-intrinsic ports confirmed for the asymmetric class, STARTER = {0,2,4} balanced; spread-class kinds are *bimodal* (CURVE ≈87/13 dominant-even), TURNTABLE alone uniform (the piece that rotates in play); zero unmatched retainers in the whole corpus; 3,141 balcony-attached exits banked for #4; THREE_ENTRANCE_FUNNEL = {0,2,4} (#8 evidence). **#15 gained its first mechanism candidate derived from independent data** (even-only starter ports ↔ live-direction parity), declared strictly as a to-be-predicted hypothesis. Locked: port-record storage (raw side + rotation + level; rel derived, never stored) and corpus policy (outside repo, refetchable; scripts committable, binaries not). Process: the desktop Filesystem MCP died mid-session (schema-dialect rejection post-reconnect); mid-session device-VM folder grants carried the whole close. Artifacts delivered in-conversation: two findings reports, `ports.jsonl` (43k records), analysis, miner. → #15 prediction protocol; item 5 (SkyTrax schema + miner/tests/harvest-script commits); zoom probe when renders resume.

- **2026-08-21 (s21 — the plate was the missing term; #15 and #3 answered):** Cowork surface, device-VM folder grants for the repo and the corpus plus a networked cloud clone for the red-green loop; renders and pushes from Colby's shell as always. Open ran five-for-five, with both claude.ai fields diffing `IDENTICAL` for the fourth consecutive session — and for the first time as a genuine read-back, since the deployed fields arrive in the session's own context rather than from memory of having pasted. **The open check found the session's first problem:** `mine_ports.py`, `ports.jsonl` and `analysis.txt` were on no disk anywhere — s20's best output existed only as chat attachments plus an ephemeral sandbox that happened to survive three idle days. Recovered by asking the s20 conversation to write the actual bytes through the bridge, hash-verified on arrival (observation #24). **Then the corpus answered the question the handoff had planned to render for.** GOAL_RAIL has **zero** port observations in 43,375 — not thin, structurally invisible, because it carries its own rail and so never appears in a `RailConstructionExitIdentifier` — which killed the planned protocol and forced a different derivation. Working the parity hypothesis forward showed it predicts three live directions per row where the table has two and one, and that the table does not rotate with the starter, so the missing term had to be world-fixed. One corpus probe over 640 courses found it: `BASE_LAYER_PIECE` occupies exactly 30 local cells, local (0,0) is a **corner**, and its W/SW/SE neighbours appear 0 times in 28,494 placements — so half of every 36-cell sweep had been spent on cells that were dead by construction, and the far control `(0,2)` was off-plate too. Composed, `plate_available ∩ starter_world_ports` reproduces the measured table with no free parameters, sizes included. **Tested by prediction, in two runs, 14 cells, zero misses.** Edge (0,1) refuted the port-only model; interior (-3,2) refuted `graph.py`'s own position-blind table when **SW rendered active**. Both runs bracketed by the certified control. **The harness broke first, and the control is the only reason it was noticed**: the opening control rendered `inactive` off a screenshot of the *Android home screen* — `render_course()` never launches the app and `assert_emulator_ready()` checks the emulator, not the foreground — and the near-white frame guard cannot see a dark wallpaper (observation #17, third firing and widened). The probe now resets and settles, reusing the queue's constant. Two of Claude's own verdict functions over-claimed against their own preconditions and were caught by tests rather than renders (#19, twice). Shipped at `ba41b2f`: `probe_plate_membership.py`, `probe_plate_footprint.py`, 35 offline tests carrying both runs as regression fixtures, and `build_variant` gaining `starter_pos`. Suite 478 → 513. Still untracked: `mine_ports.py`, owed since s20. → `graph.py` plate-awareness (item 2); the add-a-plate probe for #17; the `android.py` foreground guard.

- **2026-08-21 (s22 — graph.py learns the plate coordinate; a panel grades the fix):** Cowork surface, fourth bridge shape confirmed again (device-VM grants with no network; networked cloud clone for the red-green loop; **zero renders spent**). Open ran five-for-five — both claude.ai fields `IDENTICAL` for the fifth consecutive session, and the L1 check ran against canonical via a claude-config folder grant. **s21's sequenced item 2 — `graph.py` plate-awareness — shipped** (the list below renumbers now that it is gone): new `traxgen/plates.py` (per-`LayerKind` footprints, generated from the corpus by `probe_plate_footprint.py`, with an `UnmeasuredLayerKindError` refusing the kinds the corpus cannot see); `graph.py` decomposed into the measured conjunction with claims and predictions on separate surfaces — `connection_status` answers only from `MEASURED_RUNS` (eight campaigns, per-run goal-rotation coverage recorded) and requires `layer_kind` + `starter_local_pos` with no defaults, while `predict_*` carries the model; `PlacedTile` gained `local_pos` and `layer_kind`; the probe repointed at the library. The defect was reproduced against pre-fix code (worktree at `2ee7e6a`: DISCONNECTED at ERROR for the render-certified interior-SW course) before the fix was trusted. **Colby's uncertainty about one edit triggered a five-agent adversarial panel** — regression, epistemics, maintenance, KISS, steelman-the-alternative — his fresh-reviewer principle run as infrastructure, and it earned its cost: the design held (repointing is circular, since corrected code carries the probe's own renders; retiring kills the next planned run, where `table` is the only discriminating rival at interior rot 1), while the panel caught three same-day defects the author could not see. The "frozen" rival still called `live_directions` while its docstring said otherwise; the docstring's justification ("fixtures green while measuring nothing") was *enacted* by two agents independently and refuted — 13 tests fail loudly, and the silent version needs a follow-on fixture-update edit; and a test comparing the rival against its own data source could not fail (mutation-proven: a corrupted record left it green). Fixes shipped: `TABLE_CLAIM_2026_08_10` as a true literal quotation with a literal-vs-literal transcription guard (verified red under a planted typo), the stale-prose class swept rather than the noticed instance patched, a moved-board test closing a suite-wide blind spot (every fixture had layer world position (0,0); the world/local confusion mutation had passed 546 green — now it fails exactly one test, verified), and a 6,480-cell pin that the plate rival is extensionally `predict_connection` — the live-audit-of-shipped-code property, through the winner rather than the loser. Mid-session, Colby flagged explanation density above his level — the project-instructions teaching note re-fired; recalibrated, not faded. Suite 513 → 549 on the Mac gate. → the add-a-plate probe (#17); the `android.py` foreground guard; the odd-rotation interior run.

- **2026-08-23 (s23 — the harness learns what is in front of it; the odd-rotation interior run lands):** Cowork surface, and a **fifth bridge shape**: device-VM folder grants *with* network egress (measured — `uv` fetched CPython in 9s, pypi 200, `git ls-remote` succeeded), which contradicts the s21/s22 note that the VM has none. Two constraints the old note conflated: the VM now has network but still caps each call at 45s and kills backgrounded processes, so `uv sync` cannot finish there and the cloud clone stays the red-green loop. Also newly hazardous: the mount carries a **macOS `.venv`**, so a Linux `uv sync` in place would overwrite it — `UV_PROJECT_ENVIRONMENT` outside the mount, verified by mtime. Open ran five-for-five; both claude.ai fields `IDENTICAL` for the sixth consecutive session, as a real read-back. **The foreground guard shipped at `514ceca`.** Colby's call was raise-not-auto-launch, and the fixtures were captured from the live AVD rather than written from remembered format — the launcher capture is the literal text that fooled s21. Two GraviTrax captures ~3s and ~33s after launch came back byte-identical, which *bounded* the guard honestly: it closes the launcher hole and is blind to the splash hole. Defect reproduced against pre-guard code first. **A three-lens adversarial panel found five defects in Claude's own prose** (#19's sixth firing): a byte-identity claim cited to fixtures that did not contain it (#24's second firing — the real captures existed only in this conversation), "bracketing the flow" over-claiming on the default cleanup path, "the dump is read exactly once" with no test and two surviving mutants, `~14s` of WAITS that is really 22.5s, and this session dated 2026-08-21. All repaired; three mutants that survived 567 green now fail. **Then the renders.** The first interior-rot-1 run died `INCOMPLETE` on two SSL-handshake upload timeouts that took out both controls (#15's fourth firing, a new signature — previously HTTP 520). Rather than re-run into it, `upload_course_with_retry` went into the library, retrying **by exception class** (5xx and network yes; 4xx and malformed no) with attempt counts recorded into the sidecar so the endpoint's failure rate stays countable. A `--no-render` pre-flight then landed 7/7 — and incidentally re-confirmed content-hash dedup across 16 days: the certified control returned `KN6F459ZR3`, the same code frozen in the 2026-08-07 `uiautomator` fixture, which also proves `generate_minimal()` has not drifted a byte. A second run died `INCOMPLETE` again, this time on the frame guard refusing the opening control at `white_frac 0.660` — a loading screen, the guard earning its keep a fourth time. **The stale-paste incident:** Claude was handed run 1's output as if it were run 2's, and nearly concluded the retry had failed; the tell was a prose detail (the message format changed with the retry), and the sidecar settled it. Runs now print a UTC timestamp. **The third run: `MODEL_SURVIVES:plate+port_only`, 7/7, bracketed both ends plus a local control.** W and SE active at an interior odd rotation; `table` dead on both; `MEASURED_RUNS` to nine campaigns and the class sweep confirms the conjunction reproduces all of them. Suite 549 → 580. → the add-a-plate probe (#17); pixel-stability polling; SkyTrax.

- **2026-08-24 (s24 — the library learns multi-plate; a panel finds the ERROR it made reachable):** Cowork surface, fifth bridge shape confirmed by **re-measurement at open** rather than by reading `environment.md` (observations #28 applied the session after it was written): pypi 200, `git ls-remote` succeeded, `adb`/`emulator` absent. Open ran five-for-five; both claude.ai fields diffed `IDENTICAL` for the seventh consecutive session — **with a caveat Claude raised itself**, see new candidate #29: the transcription of the deployed field was written *after* reading the canonical copy, so the two sides shared this context and the diff is weaker than the streak implies. The fix is a one-line ordering change, not a new check. **Colby chose the corpus phase of #17 first, which was the right order and cost nothing.** 640 courses parsed (tally matching `plates.py`'s recorded provenance exactly, an independent check that the same corpus was read): 599 multi-plate, **zero footprint collisions across all 640**, 120 distinct plate deltas, and 228 of 241 four-plate courses on one set — `plates.STANDARD_SQUARE`, emitted by the new `scripts/probe_plate_arrangement.py`. Two facts found in that phase would have silently confounded the render: **empty plates are ordinary** (563 of 4,599 carry zero cells, so an added bare plate is not degenerate), and **`layer_height` is -0.2 in 4,598 of 4,599 real plates** while `generate_minimal()` uses 0.0 — a confound axis nobody had noticed, now a locked decision with the parameter to change it. **Claude pushed back on the handoff's probe design and the pushback held**: "add a plate and re-render" changes plate presence and address validity together, so it cannot separate #17's two readings; the replacement 2×2 is locked. **The library gained `traxgen/layout.py`** — explicit-layout course building, with the single-plate case pinned byte-identical to `generate_minimal()` — after a check showed the write path was the only gap (serializer, parser and validator already handled N layers; only `generate_minimal` and `build_variant` were hardcoded to one). **The panel is now unambiguously standing procedure and it paid the most it ever has** (#19's seventh firing): three lenses on finished, green, self-reviewed work returned six false sentences, 28 surviving mutants out of 63, and — the finding that mattered — **a false ERROR that Claude's own work had made reachable**. `START_GOAL_CONNECTED` fired at ERROR on the exact 2×2 arm, because `classify_pair` only sends *cross-layer* pairs to `UNMEASURED` and every `MEASURED_RUNS` row was rendered on one plate. Reproduced against pre-guard code, fixed at `start_goal_status`, two regression tests. Nine of the ten retested mutants now fail; the tenth was **conceded rather than papered over** — measured, `(8,-6) → (9,-6)` stays contiguous and collision-free, so no offline test can distinguish a corrupted `STANDARD_SQUARE`, and the test docstring says so. Six false sentences repaired, including one where a test claiming to pin the superseded probe's defect graded its own copy of the filter (#12 again) and one where the byte-identity test's docstring claimed a link to FLW4TMLP5V's bytes that are not in the repo. Suite 580 → 619, ruff clean on s24's files (the 72 elsewhere re-counted as inherited), Mac gate green, `30dfb4d` pushed. **Zero renders spent.** Mid-session Colby flagged explanation density for the second time in three sessions — captured as candidate #31, with the observation that the project-instructions note scopes it to *testing concepts* while neither overrun was about testing. → the #17 2×2 (item 4), gated on pixel-stability polling (item 5); the three panel recommendations (item 7), of which `MeasuredRun`'s plate set matters most and belongs before renders.

- **2026-08-24 (s25 — the record learns which board it was measured on; a panel catches the fix reopening the defect):** Cowork surface, bridge re-measured at open rather than read (observations #28): pypi 200, `git ls-remote` OK, `adb`/`emulator`/`pytest`/`ruff` absent from the VM, so the cloud clone stayed the red-green loop and the Mac stayed the gate. Open ran five-for-five. **Both claude.ai fields diffed `IDENTICAL` for the eighth consecutive session, and for the first time the check's logic holds**: candidate #29 was applied — the deployed fields were transcribed to a file *before* any canonical file was read, then compared by `md5` rather than by eye. An apparent byte-count mismatch (298 vs 300, 4026 vs 4056) was reconciled rather than waved past: Python characters against `wc` bytes, i.e. em dashes and curly quotes. Imports-vs-folder and placeholder checks were run by generating the list rather than trusting the handoff's count — three orphans confirmed, matching. **The session's first finding preceded the first line of the fix.** s24's multi-plate guard counted `BASE_LAYER_PIECE` alone while `b8052e4` and `validator._BASEPLATE_LAYER_KINDS` count `BASE_LAYER` too; enacted, the certified course plus one empty `BASE_LAYER` returned `CONNECTED` from a single-plate record. A corpus scan then measured that no real course uses `BASE_LAYER` at all, so the hole was latent — which is the argument for one shared classification, not for leaving two. `BASEPLATE_LAYER_KINDS` moved to `plates.py` (a per-`LayerKind` fact, unlike `STANDARD_SQUARE`), imported by both; the circular-import reason for the move was verified, not assumed. **Item 7(b) shipped**: `MeasuredRun.plate_offsets`, required with no default, part of `measured_run`'s key; `start_goal_status` counts nothing. **The absolute-vs-relative fork is the one worth remembering** — absolute world positions were the first implementation and they silently kill the s22 moved-board fixture, since the honest reading and the frame-confusion bug both become `UNMEASURED`; relative-to-the-starter's-plate keeps it, and names an assumption the model has always carried unwritten. **The three-lens panel then found that the fix had reopened the s24 defect one shape over** (#19's eighth firing, first convergence of two lenses on one finding): `classify_pair` claims `DISCONNECTED` for non-adjacent pairs before consulting the record, so with the guard moved from `start_goal_status` down into `connection_status`, every non-adjacent pair on a multi-plate course was claimed measured-disconnected at ERROR. Green throughout, because the one multi-plate fixture used an adjacent goal. The panel enacted the fix as well as the finding. Its mutation lens enumerated 16 fixture coincidences and left 22 survivors, most tracing to one: every offset the suite compared was the zero vector, so seven distinct mutations to the rebase all survived. Its epistemics lens found `build_variant` credited with a campaign that predates it by two days (`_goal_variant`, `8929ad0`) and a corpus figure with no committed producer. **Then Claude's own mutation battery reported 0 of 19 caught, including its control** — a nonexistent pytest flag made every run collect nothing and "no `FAILED` lines" parsed as "survived"; new candidate #32, and the reason the control now halts the battery instead of printing a row. Repaired: **17 of 19 caught**, the two survivors conceded in writing (one provably equivalent, with the condition its equivalence rests on now pinned by a test; one a filter clause dead until the 2×2 adds a multi-plate row). `scripts/probe_plate_arrangement.py` gained `kind=` tallies over every layer — the footprint probe counts kinds only from within cells, so an empty layer is invisible to it — and the run reproduced every cited corpus figure exactly (640 parsed, 599 multi-plate, 0 collisions, 228 of 241, 4,599 plates, 563 empty, 4,598 at -0.2) plus the new one: **zero `BASE_LAYER` layers in 640 courses**. Eleven new tests, five decisions locked, five observation firings. Suite 619 → 639, ruff clean on this session's files, Mac gate green. **Zero renders spent.** Mid-session Colby raised the **board-state framing** — treat this as a game with a board, and ask how the board is determined and how pieces are added or removed — captured into `vision.md`. Density was not flagged this session; leading with the plain-language version was applied deliberately (see #31, which remains Colby's call). **Post-close addendum, and it is a rule rather than a note:** Colby raised that he is hitting usage limits and proposed running locally whatever can run locally. The stated lever was the wrong one and the session measured the right one instead of arguing — `/explain-usage` over this session's own transcript, ~16.8M effective tokens across 269 turns. Findings: the standing system prompt and tool list are **22.5%**, re-read every turn regardless of its content, so many small turns cost more than the same work in fewer larger ones; **reading files cost nine times everything executed on the Mac combined**, so *where* compute runs barely matters while *how much output crosses into context* is nearly all of it; and the three-lens panel was the single largest line item at **27%**. Claude's own earlier figure for the panel (~465k) was the tool-reported subagent count rather than its cost and was low by roughly 10× — #12's shape again, in a number offered in conversation. Landed as two canonical edits: a *Context is the scarce resource* bullet in `global_preferences.md` with three rules and a cheap test, and a cost-and-scope clause on that file's adversarial-reviewer bullet; plus a project-specific panel clause in this repo's `workflow.md` Working notes (when to run it here, when to skip, and the three lenses that have worked). **`global_preferences.md` changed, so the Custom Instructions field owes a re-paste.** → item 5 (pixel-stability polling); then the #17 2×2, whose arm-1 recordability is now an open design question (item 4).

## Cross-references

- `project-instructions.md` — identity, purpose, mode, and domain context (Layer 2).
- `workflow.md` — the session routines (open, drift-check, close, capture, handoff).
- `decisions.md` — locked choices, with the reasoning behind them.
- `observations.md` — recurring process patterns.
- `vision.md` — the project's longer-arc direction.
- `knowledge/` — project-scope reference material (Layer 3).
- `docs/refs/` (repo) — the project's committed reference corpus, indexed by its README.
- Archived: `docs/PLAN.md` — the pre-Allostatik living document; historical detail beyond what the files above carry.
